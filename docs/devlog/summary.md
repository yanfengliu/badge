# Devlog Summary

- 2026-09-02: **Made Badge Studio a per-badge editor opened only from Discover** — it now adjusts exactly one badge's picture, shape, material, border colour and width, tags, quote and collections, and its candidate generation, prompt library and pack publishing are gone; new badges are authored into the catalogue by chatting with Claude. Adjustments are an Archive-local `BadgeAdjustment` overlay rather than an edit of `publishedVisual`, so a catalogue reseed or visual upgrade refreshes the shipped badge underneath them and clearing one restores the exact default; activation folds the effective visual into the record and seals the face and quote while tags and collections stay editable. Backup, restore and recovery closure widened to art no catalogue can hand back, so an owner-supplied image travels even on an uncollected badge, and `adjustment` joined the saying request's excluded fields, moving the disclosure fingerprint to `sha256:6a4d1f50…`. Looking at the rendered result found five defects the suite did not: a cold `#studio/<recordId>` never opened the archive at all, a save then activation was refused as a changed visual, the construction bench scrolled away, the phone header collided at the `320px` floor, and a stacked tag field grew to `220px`. Gates: `170` test files, `1058` passed and 1 skipped; five mutations run and reverted. Coverage fell where Studio's compiler was deleted — `catalogue-lineage.test.mjs` now re-admits shipped bytes instead of comparing two independent compilers, and its header says so.
- 2026-09-02: **Bound every promoted national-park source to a native-resolution review of its own bytes** — retired the repo's only lesson into `native-source-review.test.ts` plus a `native-source-reviews.ts` register recording, per source, the sha256 inspected, the pixel size it was inspected at, and what was checked, so a regenerated source strands its review by name instead of inheriting it. Proved the anchor's own named gate does not cover the lesson: copying `kings-canyon.jpg` over `sequoia.jpg` and refreshing its digest — exactly what a regeneration plus integrity refresh produces — leaves `us-national-parks.test.ts` at **4 passed / 4**, green on an image of the wrong park, while the new gate names Sequoia. Three mutations run and reverted (stranded digest, missing record, review recorded at `128px`). Seeding the register meant doing what the lesson demands: all `63` sources inspected at native `896 × 896` against each cue, its explicit exclusions, and all four edges — all full-bleed and text-free, three carrying notes (`gateway-arch`, `mammoth-cave`, `voyageurs`), none rejected. Also measured that the `2026-08-27` code-native rerender replaced every park source _after_ the `2026-08-23` native audit and was itself reviewed on exact-`48px` proof sheets, so the anchored Sequoia digest `f3743d43…` is not the promoted `875acb49…`. The aggregate-view half has no mechanical trigger and is staged in `canon-candidates.md`. Gates: `178` test files, `1077` tests passed and 1 skipped; typecheck, lint, boundaries, format and build green; `check:docs` red before and after this change on the seeded fleet constitution differing from `../fleet/FLEET.md`, which this repo does not own.
- 2026-08-31: **Made Claude Preview setup shared and fixture-safe** — tracked the two unified-host launch configurations and routed both disposable fixture origins through npm lifecycle scripts so a fresh clone cannot open the `5174` fallback with missing or stale generated fixtures.
- 2026-08-31: **Pinned same-set navigation outside changing badge content** — preparation now puts its pager before the variable title, replay keeps it above an independently scrolling memory body, and fixed `44px` edge tracks hold both step controls at identical coordinates through title wrapping, `1 → 10` index changes, long notes, and compact layouts.
- 2026-08-31: **Gave every built-in badge its own historical quotations** — all `350` definitions now have private two-choice banks with `700` globally disjoint source-backed texts and defaults; quotation disclosure is contract v6, duplicate updates and activations are transactionally blocked, and legacy unearned collisions reconcile deterministically while sealed history remains byte-exact.
- 2026-08-28: **Kept the Studio own-image swap available across publication** — a validated post-freeze upload now opens a replacement-edition draft while the exact frozen release remains re-offerable; Studio still receives no Archive activation data, and no earned visual, date, note, or quotation is silently rewritten. Explicit Archive application remains unimplemented.
- 2026-08-28: Made the already-initialized Collection usable in `2.825s` instead of `11.460s` on the controlled `8 Mbps`/`100ms` profile — the `647,968`-byte Brotli monolith became `200,687` bytes of startup code with Studio and live 3D deferred, normal source fetch fell from `7,996,418` to `1,044,652` bytes, and independent review closed unvalidated-byte caching, stale recovery-state, unearned-legacy overfetch, chunk-failure, layout-shift, contrast, and incomplete-budget traps.
- 2026-08-27: Finished the deterministic code-native miniature rerender for all 347 authoring projects, added a rights-bounded fifty-game edition with 150 candidates across 24 authoring directions and 14 realized construction families plus an activatable alpha.2 catalogue pack, raised the measured media ceiling to 384, preserved earned archives through version-gated reconciliation, and repaired compact fallback containment after real-browser review exposed both overflow and a zero-sized container-query cycle.
- 2026-08-27: Made the full suite trustworthy under parallel CPU load — replaced exact attempt-count and last-error-identity pins in the live-filesystem publish and lock gates with retry lower bounds, the shared 8-attempt ceiling, and a load-exact no-rename-after-publication invariant; gave the heavy PowerShell, multi-asset decode, and repo-sweep gate class 60-second budgets; and proved three consecutive green full runs under a sustained 16-process spin load after a 32-process census exposed the 13-member class.
- 2026-08-27: Gave Discover an origin-aware `Back to Collection`/`Back to Timeline` return for every cross-section handoff and made both single-badge surfaces traversable — preparation pages the exact filtered grid sequence and replay pages the set's collected memories, with visible 44-pixel steps, plain-arrow keyboard equivalents that yield to text fields and the engaged viewer, truthful earned-state rendering, and above-the-fold placement fixed after a screenshot disproved the DOM-verified first layout.
- 2026-08-27: Retired the consumerless 384-pixel discovery detail tier end to end (D-056) — glob, Vite tier, bundle-gate sections, manifest, derivation and decode scripts, and 297 tracked assets — shrinking the archive build by 297 files and 8,136,025 bytes to two integrity-bound JPEG tiers.
- 2026-08-26: Made all 300 Discover badges activatable in place through a generated digest-bound discovery fixture pack with reviewed quotation banks and JPEG durable-source admission, collapsed Discover to wordless collected-color versus not-yet-collected-grey cards, shipped the bounded canonical-source bundle tier, reconciled older archives and backups by version-gated appends plus unearned-lineage reseeding, and hardened the round with a three-lane adversarial review that reshaped recovery, repair, and JPEG admission before commit.
- 2026-08-26: Made the owner-preferred luminous ligne-claire science-fantasy direction a machine-readable frequent choice for future multi-badge campaigns, applied it to 14 candidate trios and six primaries in the 57-item future idea bank, and hardened fresh fixture publication against bounded transient Windows scanner holds.
- 2026-08-26: Replaced three generic Michelin regions with 132 named, source-audited restaurant achievements—41 across the nine-county Bay Area, 69 in New York City, and 22 around Washington, DC—using concrete interior, dish, or culture cues, broad manufactured geometry, exact miniature and structural-diversity gates, a bounded 297-thumbnail and lazy-detail Discover delivery, a responsive read-only Visual basis preview, and a corrected Collection handoff that always opens the complete set.
- 2026-08-25: Expanded Discover with fifty book studies, two education milestones, and three generic regional Michelin-dining studies under the v2 miniature-manufacturing and historical-quotation standards; preserved the no-directory rights boundary; and replaced the growing wrapping taxonomy with one bounded horizontal set rail.
- 2026-08-25: Replaced six illustration-dense fixture faces with manufacturing-reviewed cloisonne, embroidery, marquetry, cut-paper, ceramic, and relief-print compositions; added a measurable miniature prompt and proof contract; advanced the starter to alpha.4; and reconciled only exact unearned legacy visuals while preserving earned editions and source bytes.
- 2026-08-25: Moved Badge Studio from its own `/studio` document into the `#studio` fourth primary section of the root host while retaining separate private stores, repositories, backups, and standalone isolation builds and requiring versioned root, usable-entry, and canonical-redirect proof before launcher reuse.
- 2026-08-25: Made every Discover badge a full-card pointer and keyboard action, routing collected cards to exact memory, published potentials to preparation, and unpublished studies to a bounded read-only preview with truthful capability limits.
- 2026-08-25: Made Archive and Badge Studio complete at a 320-pixel phone floor with safe-area and dynamic-height layouts, always-visible four-destination navigation, deliberate touch rails, bounded portrait and landscape surfaces, touch-versus-page-scroll arbitration in the live viewer, responsive contracts, and canonical phone references.
- 2026-08-25: Added a sourced and visually reviewed fifty-state discovery edition across twenty-four art styles, exact historical-quotation defaults, progressive searchable Discover scaling, represented-only Collection shelves, truthful future catalogue plans, and a durable UI visual record.
- 2026-08-24: Made each canonical Collection shelf itself the Discover set action with a full-segment hover and keyboard-focus highlight, while keeping badge replay and disclosure controls independent and removing the redundant `Browse set` label.
- 2026-08-24: Rebuilt Collection as an earned-only fancy field cabinet with closed set shelves, search and derived stats; made Discover the complete color-versus-grey set browser; added exact memory replay and set drill-down; and aligned Archive and Studio on four primary destinations.
- 2026-08-24: Standardized unearned starter legacy sayings to curated historical quotations, preserved earned memory exactly, added separately bound English-Wikipedia biography provenance to quotation contract v3, and split figure and quotation-source presentation into two rows.
- 2026-08-24: Removed the angle-dependent triangle lattice from wool badges with irregular deterministic microfibers, gentler bump response, bounded oblique sampling, and a full short-period material gate.
- 2026-08-24: Simplified earned-state activation replay to one passive 360-degree badge turn with no inspection, scale, or reset controls and a static reduced-motion or renderer-fallback form, while preserving first-activation inspection.
- 2026-08-24: Moved each Studio exact compiled prompt into a native disclosure that starts collapsed while preserving keyboard expansion and local copying.
- 2026-08-24: Added Archive Discover for all 66 unique visualized badge concepts, with exhaustive safe projection tests, truthful published-versus-study status, search and filtering, available-only Collection navigation, and an exact bounded-thumbnail bundle gate.
- 2026-08-24: Replaced saying authoring with preseeded, source-checked historical quotations and one regeneration action, backed by exact-ID selection, record-bound admission, durable quote revisions, and truthful sealed-memory recovery.
- 2026-08-23: Established a sourced Studio-only authoring campaign for all 63 national parks with natively reviewed source studies, deterministic parent-derived thumbnails, contained media-repair states, three candidate directions each, 24 immutable diverse art styles, exact prompts, and 57 common-achievement ideas without claiming pack publication.
- 2026-08-23: Recast Timeline pictures as exact recipe-rendered badge artifacts with one bounded live inspector, and polished Replay activation into a dedicated focus-stable earned-memory coda.
- 2026-08-23: Replaced the disabled Timeline placeholder with an elegant read-only chronology of earned memories, deterministic real-world date ordering, exact-memory reopening, an empty state, and responsive Archive navigation.
- 2026-08-23: Expanded saying prompt v2 to compact new paragraphs and source-checked historical quotations selected only by exact ID, with exact request rebinding, explicit attribution, disclosure, and separate generated-versus-accepted safety limits.
- 2026-08-23: Connected explicit Archive saying proposals to a bounded same-listener Claude Code adapter with exact no-provider-model-call disclosure, pinned provenance, actionable expired-auth handling, and no API key or fallback.
- 2026-08-23: Separated explicit fixture-backed saying proposals from accepted lines and added the exact theme-aware runtime prompt, provider-neutral controller, and privacy boundary.
- 2026-08-23: Removed the pre-release two-origin recovery detour after the owner clarified that no such installation or user data exists; one-site startup now has one state record and one command.
- 2026-08-23: Unified Archive and Studio under one remembered local site at `/` and `/studio/`, and isolated launcher verification from canonical `.badge-local` state.
- 2026-08-23: Made local startup idempotent and collision-tolerant with Badge-instance reuse, one remembered free Archive and Studio port pair, dynamic companion links, and origin-continuity refusal after selection.
- 2026-08-23: Shipped the first runnable local Archive and developer-only Studio slice with four crafted badges, qualified-identity and state-preserving recovery, bounded non-destructive image work, dependency-closed pack-and-theme publication, and live 3D or static fallback inspection.
- 2026-08-22: Split visual authoring into a developer-only Badge Studio and made the everyday Archive consume only validated, immutable packs whose artwork and 3D presentation are already decided.
- 2026-08-22: Recorded the owner's standing authorization for ordinary verified pushes to `github.com/yanfengliu/badge`, excluding force-pushes, history rewrites, other remotes, and gate bypasses.
- 2026-08-22: Promoted badges to real-time 3D artifacts with dynamic lighting, mouse examination, engine-neutral saved recipes, accessible controls, and GPU recovery or fallback requirements.
- 2026-08-22: Clarified Badge Atelier saying authoring: generated retries are independent, non-destructive proposals, while direct writing remains first-class and authoritative.
- 2026-08-22: Nothing user-facing changed. Established the durable product, visual, privacy, persistence, media, backup, and roadmap contracts; preserved the selected gallery and Badge Atelier directions as optimized design inputs. Runtime generation credentials and repeat-achievement presentation remain explicit decision gates.
