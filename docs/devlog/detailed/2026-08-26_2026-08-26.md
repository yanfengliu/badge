# Devlog — 2026-08-26

## Michelin Dining became a source-audited 132-restaurant edition

**Owner correction:** The owner clarified that Michelin badges must name specific starred restaurants and derive their art from each restaurant's actual interior, dishes, or culture rather than representing three generic dining regions.

**Official inventory and source ledger:** The checked `2026-08-26` edition records `132` restaurants from the live Michelin Guide selections used by Badge: Bay Area `41` under the Metropolitan Transportation Commission's nine-county boundary, New York City `69`, and Washington, DC and surroundings `22`. The independent [Michelin inventory provenance ledger](../../design/michelin-inventory-provenance.md) records each name, locality, checked star count, individual official Guide URL, source list page and order, regional range, and distinction totals without importing the production seeds, so a missing, duplicated, reordered, or rating-drifted entry cannot hide behind the authoring projection.

**Concrete-source audit:** A record-by-record audit rejected the seven remaining generic entries and confirmed that all `132` records contain a concrete dish, ingredient, technique, cultural practice, named chef, or distinctive interior feature. The repaired controls include Benu's maple-soy whole quail with sweet-potato crepinette and black truffle, Nisei's omurice and nori maki, Lazy Bear's paired oysters beneath its lodge-like warehouse frame, Wolfsbane's Dungeness crab and blush Maltaise, YingTao's pork-and-prawn wontons beside its curved counter, Café Boulud's potato-wrapped sea bass in an Art Deco frame, and Essential by Christophe's three blue prawns and brass-beige room. A reviewed SHA-256 over every research row plus nine explicit cue-and-source controls makes later generic drift visible offline.

**Achievement and quotation contract:** Every achievement is titled `Dined at [restaurant]`, requires a visit while the linked Guide page lists at least one Michelin star, and binds a paraphrased, source-checked interior, dish, or cultural cue without copying Guide prose, photography, logos, rating marks, restaurant branding, menus, or chef likenesses. The edition pre-generates the source-checked Oscar Wilde quotation “After a good dinner one can forgive anybody, even one’s own relations.” with its Project Gutenberg evidence page and separately bound English-Wikipedia biography URL; the selected study remains read-only and does not gain runtime activation authority merely by appearing in Discover.

**Art investigation:** The first code-native restaurant renderer still depended on natural-language motif classification, could ignore multiplicity, accepted unsupported geographic shorthand, allowed heuristic palette drift, and treated some style choices as metadata that did not change the rendered pixels. Independent criticism showed that source records and diverse labels were insufficient evidence when the actual geometry could remain generic.

**Manufactured geometry correction:** Each restaurant now has an explicit art profile that binds its exact research cue to a limited hexadecimal palette, `3–5` broad motifs, normalized geometry, requested count, source label, spatial relationship, edge strategy, and one of `14` rendered manufacturing signatures. The renderer maps those explicit motif kinds into production-scale flat geometry and gives every motif exactly two motif-bound commands, avoiding copied pixels, typography, finished badge construction, and miniature scenic narration.

**Miniature and diversity evidence:** All `132` raw `896 × 896` renders pass the exact `48 × 48` reconstruction ceiling of `0.045` before normalization, and every researched motif changes at least eight pixels while retaining at least `15%` of its standalone footprint. The style gate's nearest RGB and palette-neutral categorical pairs measured `0.031110` and `0.052010` against `0.03` and `0.045` floors; the all-pairs geometry gate measured `0.103465` for the nearest of `8,646` restaurant pairs and `0.084200` for its occlusion control against the `0.045` floor. All final sources fully decode below `256 KiB`, with Selby's largest at `46,679` bytes, and a complete exact-size proof sheet exposed all `132` faces together for visual rejection of microtexture, hidden motifs, or shared templates.

**Verification ownership:** The generic code-native miniature gate had begun recomputing all `132` restaurant residuals after the restaurant-specific gate had already measured them, leaving one synchronous worker CPU-bound for more than eight minutes without increasing coverage. The generic gate now owns the `45` non-restaurant recipes while the stricter restaurant gate owns the exact `132` restaurant population, motif visibility, style separation, and all-pairs geometry checks. Two native full-decode measurements also grew just beyond Vitest's generic five-second limit under the expanded catalogue, so those bounded measurements now declare the same explicit twenty-second ceiling already used by the complete restaurant miniature gate. Independent final review also caught nine expanded authoring, fixture, and gate modules at or above the repository's `500`-line target; ordered public aggregators now compose bounded profile, restaurant, Bay Area fixture, style, and geometry partitions without changing the edition order or assertions.

**Integrity and Discover media:** Exact provenance distinguishes direct code-native restaurant sources from retained canonical image generation and code-native replacements, binding the canonical prompt association, serialized design brief, renderer implementation, normalized source, and list derivative without inventing an image-generation predecessor. The production Discover manifests now contain `297` qualified thumbnails totaling `1,258,975` bytes and `297` on-demand `384 × 384` detail previews totaling `8,136,025` bytes. Exact key, hash, full-decode, size, and no-source-leak checks remain in place, and Discover mounts only `24` matching cards before an explicit bounded reveal.

**Discover experience:** Michelin Dining reports `0 / 132 collected`, preserves potential badges in the established grey treatment, supports restaurant, cuisine, locality, region, and accent-insensitive search, and makes the complete card the read-only preview action. Region filters expose the exact `41 / 69 / 22` Bay Area, New York City, and Washington populations. The study dialog labels the canonical individual Guide page only as the official Michelin listing; when a distinct restaurant menu or interior source informed the cue, it exposes that URL in a separately labeled reviewed-visual-evidence row rather than falsely crediting the Guide listing for every visual detail.

**Collection handoff correction:** The whole-surface Collection set action selected the correct Discover set but retained an older Discover query, so a prior `Yosemite` search could hide every potential park after the owner clicked the U.S. National Parks shelf. The mounted real-application flow now carries that stale query through activation and replay before requiring the shelf handoff to clear it; headless Chromium confirmed the repaired destination at `/#discover` with an empty search, one colored collected card, twenty-three grey potential cards in the first bounded page, and `1 / 64 collected` progress.

**Responsive and renderer sweep:** Headless Chromium reviewed Michelin Discover and its source dialog at `1440 × 1000`, `320 × 568`, `480 × 320`, and `844 × 390`; Collection at `1440 × 1000` and `320 × 568`; Timeline and Studio at the `320`-pixel phone floor; and the Studio renderer at front, both edges, back, alternate light, and `121%` zoom. The reported wool rectangle case was repeated at front, oblique, five-degree grazing-light, edge, and back angles without the former triangle lattice. First activation retained Inspect object, Adjust light, bounded zoom, and both resets for deliberate artifact examination. Earned replay recorded the exact `waiting → running → complete` one-turn sequence over about `2.4` seconds with zero viewer controls, became static under `prefers-reduced-motion: reduce`, and kept zero horizontal overflow. The forced `?fallback` surface mounted no canvas and preserved clean front, edge, and back inspection. A clean post-split browser session repeated the Café Boulud provenance dialog, `320`-pixel and short-landscape containment, interactive first activation, control-free replay, reduced motion, and fallback with zero errors; only the known upstream Three.js clock deprecation warning remained. The promoted Michelin overview, Café Boulud study, and current `/#studio` phone captures preserve the accepted interface direction for later work.

**Reviewer-found evidence gap:** The first final pass promoted the current `/#studio` phone capture and documented the former `/studio/` image as superseded, but the exact-dimension PNG gate still named the old file. Independent harsh review caught that the documentation claim therefore exceeded the instrument. The canonical mobile reference population now replaces the old Studio path with `studio-mobile-root-2026-08-26.png`, so corruption or dimension drift in the current single-root capture fails the gate.

**Replay reference closure:** Final harsh review found that the materially changed passive replay had been inspected but still lacked a promoted canonical frame. A fresh fixture-only `1440 × 1000` pass captured and inspected the live turn at an `850ms` edge angle, the final `complete` pose, the static `reduced-motion` pose, and the zero-canvas `?fallback` pose with exact-width measurements; the complete frame is now the optimized `archive-replay-2026-08-26.png` current implementation reference, and a full-decode gate freezes its dimensions and byte ceiling.

## Luminous ligne claire became a frequent owner preference

**Owner direction:** The owner named Moebius as a loved visual influence and asked for that language to recur frequently. Badge maps that taste to its existing creator-neutral `luminous-ligne-claire@1` family—clean contour, vast quiet space, luminous color, and journey-like science-fantasy scale—so future prompts can carry the qualities without requesting imitation or copying characters, vehicles, worlds, or compositions.

**Cadence and boundary:** New curated campaigns of at least eight concepts now target luminous ligne claire in roughly `25%` of candidate trios and `10%` of primary selections when it fits the subject and survives the miniature manufacturing contract. The unselected common-achievement queue is the first consumer: total eclipse and shipped-app directions raise candidate coverage from `12 / 57` to `14 / 57`, and the cross-country-road-trip direction raises primary coverage from `5 / 57` to `6 / 57`. The cadence does not mutate recorded candidate sets, selected-source hashes, published art, or earned memories; the queue retains at least `18` primary styles, caps any one primary style at `15%`, and requires subject-specific motifs rather than claiming unimplemented per-idea palette or viewpoint variation.

**Contract evidence:** `art-styles.test.ts` failed first because the owner preference had no executable representation, then passed after the frozen preference record and exact-count helper were added. `common-achievements.test.ts` then failed on the real queue's prior `12` candidate appearances before the three subject-fitting adjustments made it satisfy the calculated `14` candidate and six primary targets. The gates exempt campaigns below eight from a quota, reject invalid counts, leave the creator-imitation detector over the complete prompt-facing style library intact, and document that future code-native primaries need visible clear-line geometry rather than a metadata-only label.

**Verification blocker and search:** Two otherwise complete full-gate runs failed in different isolated test roots when Windows returned `EPERM` while renaming a freshly generated staging directory to its absent public fixture path. A hard-problem search separated filesystem semantics, Vitest interference, and held-out stress: distinct worker roots ruled out an internal output collision; the later undefined-tree assertion was proved to be cascade noise from the first sequential test; one branch reproduced the coupled failure once in six runs, while 52 concurrent process-level invocations and 520 focused tests produced no failure, proving ordinary load was not a reliable reproduction.

**Bounded publisher correction:** The fresh publish now applies the same eight-attempt `EPERM` / `EACCES` / `EBUSY` class and capped exponential backoff already used for lock transitions, retries transient exact-tree validation after the directory has moved, accepts an ambiguous-success state only when that validation passes, and otherwise preserves the primary cause in a specific error. Fault-injection contracts cover first-attempt recovery, post-rename validation denial, exact and corrupt ambiguous-success trees including the eighth attempt, all three exhausted error codes with no public or staging residue, a simultaneous cleanup denial that retains the staging path, and a lock-release denial that aggregates rather than replacing the earlier failures. The existing-target per-file repair path remains a separately scoped whole-edition atomicity limitation rather than an overclaim of this fix.

**Adversarial review:** The required external CLI review could not produce a grounded result: Claude Fable rejected an expired OAuth session, the sandboxed Codex reviewer hit a certificate failure, and the approval boundary correctly rejected a retry that would export repository content without explicit user authorization. Three independent read-only in-thread review lenses replaced that unavailable path; they caught the impossible staged-file recovery wording, cleanup and lock-release error masking, the missing post-rename validation budget, and the missing final-attempt ambiguous-success gate, and each issue was closed before the final full verification.
