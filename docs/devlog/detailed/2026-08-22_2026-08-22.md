# Devlog — 2026-08-22

## The product became a catalogue plus a private achievement ledger

**Timestamp:** 2026-08-22

**Action:** Consolidated the owner's discovery answers into canonical vision, product, visual, architecture, roadmap, local-policy, and agent-guidance documents before implementation.

**Result:** Future work has one authority for each subject, three canonical fixture families, two promoted visual references, a Git-versus-local data boundary, a target browser-local architecture, and twelve explicitly unresolved product decisions instead of implicit guesses.

**Reasoning:** Sibling repositories consistently separate concise agent instructions from design and architecture detail. The strongest data model separates Git-tracked achievement definitions from local personal records, which satisfies both the owner's desire to version the achievement system and the requirement that activations and artwork stay local.

**Validation:** Relative links, file shape, fleet block integrity, prose formatting, binary sizes, and the final diff are checked in this session. No application behavior exists to run.

**Code reviewer comments:** An independent critic found that collection visibility lacked a local owner, repeat cardinality had leaked into the schema, IndexedDB lacked a stable-origin contract, the fleet seed looked managed, the docs gate was manual, and recommendations looked owner-confirmed. Each issue was corrected before delivery.

**Notes:** `badge` is not yet present in `../fleet/scripts/lib/fleet-repos.mjs`, so fleet canon sync will not manage this repo until a separately scoped sibling-repo change onboards it. Do not run a broad sync from this repo as a workaround.

## Browser-local is a provisional implementation boundary

**Timestamp:** 2026-08-22

**Action:** Recorded a React, Vite, TypeScript, IndexedDB, and file-export Phase 1 target while keeping live model generation behind an adapter.

**Result:** The first offline vertical slice can proceed without a secret-bearing backend, while the design preserves a later local companion service or desktop wrapper if credential or storage requirements demand one.

**Reasoning:** IndexedDB and disk export cover the confirmed local-use and backup requirements with the smallest app shape. A privileged provider credential must not be embedded in browser code, so live generation cannot be allowed to leak across the architecture boundary by convenience.

**Validation:** D-007 is explicitly provisional, the product spec retains the runtime as an open decision gate, and Phase 1 uses deterministic candidates before Phase 2 live generation.

**Code reviewer comments:** The critic identified origin-scoped IndexedDB as a data-loss illusion risk. D-009 now reserves a strict durable origin, isolates development fixtures, and requires migration before any origin transition.

## Saying authoring became independent and non-destructive

**Timestamp:** 2026-08-22

**Action:** Clarified that Badge Atelier can generate or regenerate a one-line saying and always lets the user write the line directly.

**Result:** Saying generation now has a provider-neutral boundary separate from art generation. A retry creates a pending proposal, preserves the accepted or handwritten line until explicit acceptance, and cannot change the selected art or appearance.

**Reasoning:** The saying is part of the user's memory and voice, not an irreversible model output. Separating it from art makes retry behavior predictable, prevents an expensive or surprising art call, and keeps manual authorship first-class.

**Validation:** Product, visual, roadmap, policy, architecture, decision, accessibility, and test contracts describe the same interaction; the repository documentation gate covers the changed files.

**Code reviewer comments:** The independent critic found missing outbound-data consent, retry race behavior, reciprocal art isolation, and executable one-line validation. The contract now discloses a minimal payload, uses latest-request-wins semantics without clearing prior state, tests isolation in both directions, and defines normalization plus a provisional 120-grapheme limit.

## Badges became interactive three-dimensional artifacts

**Timestamp:** 2026-08-22

**Action:** Promoted the badge from a layered visual treatment to a real-time 3D object that can be rotated, zoomed, and examined under adjustable lighting with a mouse.

**Result:** Product and visual contracts now require genuine geometry, physical material response, front-to-back inspection, object and light modes, reset and keyboard controls, reduced-motion behavior, GPU recovery, and a multi-angle fallback. Architecture stores an engine-neutral render recipe while keeping camera, light, scene, and GPU state ephemeral.

**Reasoning:** A flat image with a hover tilt cannot create the physical weight the owner wants. Separating durable construction from renderer state preserves the local archive, permits future engine changes, and avoids checking one heavy mesh into Git for every personal badge.

**Validation:** Product, visual, roadmap, policy, architecture, decision, backup, accessibility, and test contracts describe the same 3D behavior. Documentation syntax, links, policy mirroring, whitespace, binary limits, and secret checks pass, and two independent reviewers completed clean follow-up passes.

**Code reviewer comments:** Independent review exposed first-run fallback, baked-construction source art, owner-attribution, recipe portability, cache fingerprint, input-state, keyboard, resource-lifecycle, and spike-gate gaps. The final contract resolves them with a renderer-independent fallback, source-versus-construction separation, D-011/D-012 authority split, golden recipe semantics, explicit engagement commands, idempotent session disposal and stress tests, and measurable Phase 0 exit criteria.

## Repository push authority became explicit

**Timestamp:** 2026-08-22

**Action:** Recorded the owner's standing authorization to push verified, in-scope commits to `https://github.com/yanfengliu/badge.git` without repeated approval requests.

**Result:** Future agents can perform normal non-force delivery after required gates pass, while force-pushes, history rewrites, remote changes, other repositories, review bypasses, and secret-check bypasses remain outside the authorization.

**Reasoning:** The exact destination and scope are now durable repository guidance rather than transient conversation context.

**Validation:** The documentation gate and staged-diff checks cover this policy update before commit and push.

**Code reviewer comments:** This records authorization only; it does not broaden destructive Git authority.

## Visual authoring moved into Badge Studio

**Timestamp:** 2026-08-22

**Action:** Separated badge-picture generation and all visual construction from the everyday user experience into a dedicated developer-only Badge Studio.

**Result:** Archive and Studio now have distinct target builds, origins, persistence, backups, navigation, provider capabilities, and tests. Studio generates or ingests art, compares candidates, constructs the 3D presentation, strips source metadata, validates canonical bytes, and prepares an immutable pack before explicit handoff; Archive independently admits that exact pack and exposes only the already decided visual for browsing, activation, and read-only 3D inspection. Custom work crosses from Archive only through a minimal revisioned `.badgebrief`, never database access or personal state. Open decision gates grew from 12 to 18 as saved showcase pose, renderer, pack distribution, visual reuse, Studio entry, and saying-provider choices became explicit.

**Reasoning:** A normal achievement flow should celebrate and preserve a memory, not expose the production machinery behind its badge. A data-only published-pack boundary keeps unfinished art, prompts, credentials, and provider failures out of the personal Archive while preserving rich developer tooling and user-supplied image workflows in Studio.

**Validation:** D-013 records the owner mandate, D-014 supersedes the single-package part of D-007, D-015 freezes canonical pack identity and truthful prepared releases, and D-016 freezes the revisioned semantic handoff. The documentation gate checks 18 Markdown files, 23 local links, the 66-line root `AGENTS.md`, script syntax, whitespace, and prohibited binaries. Temporary positive controls proved the staged-delivery gate rejects a fake GitHub token and a Unicode-named `.badgebrief` while accepting a clean staged file. The roadmap now proves the release boundary before either vertical slice, and product, visual, policy, architecture, backup, catalogue, test, and agent contracts use the same separation.

**Code reviewer comments:** Three independent critics found and drove fixes for residual Archive authoring language; the custom-definition handoff and its data minimization, persistence, staleness, fulfillment, and replay behavior; edit-after-fulfillment historical integrity; pack variants, canonical bytes, digests, qualified lineage throughout personal settings, records, overlays, and composites; monotonic release ledgers across uninstall and restore; same-version forks and dependency closure; backup completeness; upload metadata stripping; browser-download truthfulness; separate saying-provider authority; ignore and staged-secret gates; and durable decision and drift records. The prescribed Codex reviewer was unavailable after bounded network retries, and Claude Fable plus the required Opus fallback stalled without a verdict and were stopped; their unavailability did not replace the completed independent reviews or executable gates.
