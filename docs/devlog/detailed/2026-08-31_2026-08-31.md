# Devlog — 2026-08-31

## Quotation identity became a catalogue and storage invariant

**Timestamp:** 2026-08-31 14:50–19:10 PDT

**Action:** Replaced shared set-level saying banks with one dependency-neutral canonical text identity, one source-provenance registry containing a private two-choice bank for each of `350` built-in definitions, and transactional cross-record checks on fresh creation, quotation replacement, activation, catalogue expansion, restore, and recovery.

**Result:** The release moves from `16` selected texts and `11,266` colliding unordered default pairs to `700` globally disjoint IDs and canonical text keys with `350` distinct defaults. A symmetric close-match scan and a `17`-entry retirement ledger also prevent spelling, word-order, excerpt, and translation variants of one quotation from reaching different badges. Fresh state rejects every duplicate before writing state or source objects; existing sealed duplicates remain readable history, while unearned legacy collisions deterministically keep a free trusted value or take a free record-bound default or alternate with a fresh revision.

**Reasoning:** Request-local uniqueness could prove only that one provider shortlist did not repeat itself; it could not stop two record banks, two browser windows, or a migration from claiming equivalent punctuation, case, whitespace, or Unicode variants. The canonical key therefore lives below the saying contract, and every write checks the latest state inside the same IndexedDB transaction that would commit it.

**Validation:** The frozen corpus audit proves exactly `700` rows, `350` definitions with slots `1` and `2`, `700` IDs, `700` canonical keys, zero collisions with the `17` duplicate-prone release texts, zero symmetric close matches, all `17` adjudicated variant retirements on only their retained side, `700` exact URL-plus-SHA-256 source associations and whitespace-only source matches, and `700/700` independent pass verdicts. The tracked offline source auditor independently replays all `700` quotations against `30` exact cached sources. Six focused identity, contract, registry, and persistence files pass `95` tests, an exhaustive check of every Unicode scalar on pinned Node 24 reaches the caseless fixed point within three passes with no cycles, and the complete repository gate passes `176` test files with `1,064` tests passed and one intentionally skipped.

**Code reviewer comments:** Independent harsh review found that the first implementation wrote a fresh database seed before applying the global collision policy and that its supposed legacy test actually created the bad state through that escape hatch. Later review found an ambiguous legacy parser when a source title itself contained `” — `, capital-sharp-S and default-ignorable identity bypasses, and `17` editorial variants that exact keys missed. The corrected load path runs a stricter all-record assertion before either state or object writes, the parser reserves every plausible legacy quotation prefix, the identity deletes default-ignorable characters before its fixed-point Unicode fold, the catalogue carries the retirement ledger, and initialization storage moved into a focused helper so `repository.ts` is `491` physical lines rather than remaining over the `500`-line target. The required external Codex and Claude CLI lanes both returned HTTP 401 before review, so three independent local critic lanes covered catalogue/editorial identity, persistence/migration behavior, and the headless browser product flow; the final reviewer reported no blocker.

## Authentic quotation supply was not the same as badge-quality supply

**Timestamp:** 2026-08-31 15:00–19:10 PDT

**Action:** Fixed a held-out benchmark before expanding the corpus, saved exact public-domain source bytes, extracted literal whitespace-normalized passages, and required reviewers who did not create each candidate set to accept every promoted row as self-contained, affirmative or contemplative, memorable, and suitable beneath an achievement.

**Result:** Early grammatical and source-valid miners failed: one route exhausted at `276`, another at `651`, and successive held-out passes rejected hundreds of contextual, cynical, hostile, fragmentary, descriptive, or merely clever lines. The final assembler reviewed `735` distinct candidates, selected `700` by author round-robin across `146` people and `30` exact sources, replaced `17` independently adjudicated variants from a held-out reserve whose separate reviewer passed `45` of `50` candidates, and retained concise reasons for every selected row without editing quotation text.

**Reasoning:** Structure, attribution, HTTPS shape, and substring containment do not establish product quality. The search therefore escalated after two failed approaches, kept the done condition fixed, used deliberately different anthology, dictionary, author, aphorism, and almanac routes, and never lowered the rubric or filled a quota from rejected prose.

**Validation:** The unchanged final audit passes after the tracked registry is regenerated. A later runtime-contract gate caught three full bibliographic source titles over the `100`-grapheme limit; the assembler now validates the actual quotation schema and uses only truthful main-title prefixes keyed to the exact source URL and hash, leaving wording, person, URL, hash, and held-out verdict unchanged. A network control verified two Archive.org sources and then correctly rejected changed dynamic Wikisource bytes rather than overwriting their reviewed pin; the complete ignored exact-byte cache passes the tracked `--offline` replay.

## Integration checks found package and build gaps that source tests missed

**Timestamp:** 2026-08-31 17:00–19:10 PDT

**Action:** Routed fixtures and authoring defaults through the new registry, advanced the discovery pack to `1.0.0-alpha.3` and the live quotation disclosure to contract v6, declared direct workspace dependencies, and repaired raw-TypeScript package imports used by the host-build inspector.

**Result:** The generated discovery pack digest is bound to the regenerated private banks; `catalogue-authoring` and `catalogue-fixtures` declare the registry dependency, and Node can resolve the fixture quotation export outside Vite's source graph.

**Reasoning:** Hoisted workspace resolution and Vite transforms can hide missing package declarations and `.js` specifiers that fail when a build-check script loads raw TypeScript through Node. Package-level dependencies and explicit TypeScript self-exports make those consumers honest.

**Validation:** The single-root host-build check passes with a `200,357`-byte entry, `54,583` Brotli shell-code bytes, and `1,296,239` budgeted Archive-startup bytes while Studio and renderer chunks remain deferred. Headless browser verification showed six distinct quotations across five consecutive badges plus one regeneration; reload preserved the regenerated quotation, source, date, and note; wide, `320 × 800`, and `844 × 390` views stayed visually clean without horizontal overflow; and the clean console had zero errors and one existing Three.js deprecation warning.
