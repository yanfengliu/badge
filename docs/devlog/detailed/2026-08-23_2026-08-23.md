# Devlog — 2026-08-23

## Live sayings gained an explicit same-listener Claude boundary

**Timestamp:** 2026-08-23

**Action:** Connected the provider-neutral Archive saying controller to the owner's local Claude Code subscription through the existing Badge listener; added a session-local first-use disclosure that shows the provider, Anthropic destination, pinned model, exact prompt, exact canonical outbound values, excluded Archive fields, normalization, limits, and a fingerprint before the final explicit provider-model call; and kept manual writing plus fixture and test runs in-process and provider-free.

**Result:** The first `Generate saying` action performs only a local disclosure GET, closing the review sends no provider-model request, and only `Generate with Claude` or a later explicit `Try another` may make one bounded attempt with no retry or fallback. The browser requires same-origin transport and exact HTTP semantics, the listener validates canonical bytes and current disclosure acknowledgment, and generated copy remains a proposal until `Use this saying`; accepted or handwritten text survives failure, cancellation, stale completion, reload, and retry. Windows launches the CLI atomically inside a kill-on-close Job Object, while Unix bounds and awaits the initial process group under the documented trusted-CLI limitation. Cleanup or containment uncertainty withholds the saying, preserves evidence when required, becomes sticky, and blocks later calls.

**Validation:** The final repository gate passed 454 tests across 64 files with one Unix-only containment test skipped on Windows, all three production builds, the exact one-listener startup control, typecheck, lint, boundary and documentation checks, formatting, and a zero-vulnerability dependency audit. Mounted browser checks at 1440×1000 and 1100×900 covered the disclosure values and fingerprint, inert background, no-provider-call close, explicit provider failure, stable focus return, responsive Archive and Studio layouts, and live 3D front, edge, back, zoom, and shifted-light views; no task-owned Claude process or `badge-saying-*` workspace remained. A final headless cold-load check after disabling speculative pre-transforms loaded both real Archive and Studio entry trees with HTTP `200`, zero console errors, and visually intact 1280×720 renderings. The real same-listener probe traversed the PowerShell Job launcher and returned Claude's structured `401`, proving launch, stdout relay, fixed authentication classification, and non-destructive failure behavior, but not an authenticated success or pinned `modelUsage` envelope.

**Notes:** `claude auth status` reported logged in while the model request said the OAuth token had expired, so structured numeric `401` results now produce the fixed instruction to run `claude auth login` without exposing raw provider text. A later full gate printed startup success and released its port but retained native `FSWatcher` handles. An early import-path theory changed timing but did not survive serial reproduction; direct instrumentation instead captured Vite's unawaited HTML pre-transform adding Studio's entry module after concurrent watcher and environment shutdown had started. The shared host now disables only that supported speculative direct-import optimization, preserving on-demand transforms and HMR. After the success marker moved behind every cleanup result, the unchanged serial acceptance benchmark completed in 2,972 ms, 2,727 ms, and 2,970 ms with clean exits, closed port `4180`, and no verification state; its instrument audit also replaced an ineffective empty-directory `fs.rm` call with non-recursive `rmdir`. The canonical Claude review lane was blocked by that expired token and Codex could not validate the sandbox certificate, so the independent integrated critic supplied the required harsh review and found no remaining blocker after its mode, status, cancellation, containment, cleanup, focus, and documentation findings were gated.

## Runtime sayings became explicit theme-aware proposals

**Timestamp:** 2026-08-23

**Action:** Recorded the owner's decision that only direct `Generate saying` and `Try another` actions may invoke a serving-time model; added the exact prompt v1 and strict provider-neutral request, response, and provenance contract; and replaced the eager fixture line with an explicit Archive proposal, acceptance, and direct-writing workflow.

**Result:** The Archive now starts with no unaccepted saying, keeps its current accepted line visible while a separate local proposal is prepared or retried, persists only `Use this saying` or a valid manual save, and activates only from that accepted value. The controller makes every load, selection, activation, ceremony, reload, restore, and background path a no-provider-model-call case; cancels or rejects stale work; clears ephemeral state on restore; and keeps per-record editor saves from racing other badges. The prompt asks for concise, quietly proud, concrete, theme-related copy without invented facts or game mechanics.

**Validation:** The final repository gate passed 344 tests across 53 files, all three production builds, the one-listener startup control, typecheck, lint, boundaries, documentation, formatting, and a zero-vulnerability dependency audit. Headless Chromium exercised explicit proposal, retry, acceptance, manual authoring, over-limit refusal, per-badge draft navigation, activation blocking, reduced-motion ceremony, live front/edge/back rotation, zoom, dynamic light, GPU fallback, Studio candidate selection, immutable upload, non-destructive reprocessing, and rectangular wool rendering at 1440×1000 and 1100×900 with zero application console errors; the only warning was the known upstream `THREE.Clock` deprecation. Independent contract and UI critics signed off after their size, privacy, Unicode, stale-request, save/activation race, live-region, and hidden-draft findings were corrected and regression-tested.

**Notes:** The current Archive UI remains explicitly labeled as a curated local preview; this session implements the provider-neutral boundary and interaction without claiming that a live adapter, disclosure sheet, or credential mechanism exists. Provider and credential selection remains open decision 18.

## The one-site launcher shed its pre-release recovery detour

**Timestamp:** 2026-08-23

**Action:** Removed the separate pair-recovery command, startup lock, injected cross-origin link mode, pair-record inspection, split-surface start/development/preview aliases, and port-`4174` reservation after the owner clarified that Badge has no released two-origin installation.

**Result:** `npm start`, `npm run dev`, and `npm run preview` are the only local product surfaces for their respective modes. Startup reads and writes only the existing `.badge-local/site.json` format, serves Archive at `/` and Studio at `/studio/` on one origin, and ignores neighboring files that are not part of that contract.

**Validation:** Focused launcher contracts prove that the canonical runtime target has one state path, a neighboring `ports.json` does not affect startup, only unified local-site start/development/preview commands exist, the current one-site record remains readable, same-origin navigation has no injected origin override, and port `4174` is ordinary. The final repository gate passed `291` tests across `47` files, all three production builds, the one-listener startup control, typecheck, lint, boundaries, documentation, formatting, and a zero-vulnerability dependency audit.

**Notes:** The discarded pair file existed only as task-created verification output during the same development session. Treating it as a user migration source was an unsupported assumption; D-034 supersedes only that compatibility scope and leaves the one-site topology and separate Archive and Studio application boundaries intact. Independent review removed misleading split-surface command aliases and prevented a reset of the already-published one-site record version, which would have rejected the current origin record and stranded origin-scoped data. The first full verification printed a successful startup control but retained two Vite `FSWatcher` handles; an active-handle control identified them, and a bounded request-idle barrier followed by an unconditional server-close attempt made the same command exit cleanly without letting a failed barrier strand the listener. Shutdown-only failures now use a neutral local-run heading instead of falsely claiming that Badge never started. The canonical multi-CLI review was retried: Claude stopped on its expired OAuth token, while Codex could not validate the local certificate or reach its model service; two independent non-author critics covered launcher-surface and data-continuity lenses instead.

## Archive and Studio became one site without sharing application state

**Timestamp:** 2026-08-23

**Action:** Replaced the two-port local product topology with one remembered strict loopback site that mounts Archive at `/` and the independently built Badge Studio at `/studio/`, and separated canonical interactive launcher state from branded task-owned verification state.

**Result:** `npm start` now selects or reuses one port and one listener while both application routes remain available. Archive and Studio continue to own separate bundles, IndexedDB database names, repositories, backups, and pack handoffs; same origin is a routing choice rather than a claim that their private state is merged or isolated by the browser. `npm run recover:legacy` reopens a preserved two-origin record without creating the new site record, keeping old Studio data reachable until Studio backup exists.

**Validation:** Launcher contracts cover one-port selection, exact and mutually exclusive Archive and Studio route identity, Studio document deep-link routing, first-use fallback, remembered-origin continuity, malformed and concurrent record handling, task-target path confinement, canonical-state invariance with both absent and pre-existing records, surfaced server-cleanup failure, and terminal stop-listener disposal. The one-site lifecycle and browser controls exercise one fallback listener, `/`, `/studio/`, direct Studio reload, navigation in both directions, second-launch reuse, and isolated listener cleanup without touching canonical `.badge-local` state or unrelated processes. A separate real recovery control starts both standalone builds, follows the in-app Archive `4180` → Studio `4181` → Archive `4180` links with zero console errors, and releases both listeners; contracts additionally cover prior-validator port parity, unified-host refusal, pair startup locking, partial-lock cleanup, and listener-cleanup failure. The final repository gate passed `301` tests across `48` files, all three production builds, the isolated startup control, typecheck, lint, boundaries, documentation, formatting, and a zero-vulnerability dependency audit.

**Notes:** The earlier verification retained its ignored `.badge-local/ports.json` after using the production state path, which converted the user's first real port conflict into a remembered-origin refusal. That record was task output rather than achievement or Studio data; the corrected boundary removes verification state through its owned cleanup path and never treats the presence of a legitimate canonical record as a failure. Independent critics caught that unreadable legacy evidence could be mistaken for absence, that normal-start refusal offered no supported way to reach old Studio data, that same-origin links could strand recovery on the wrong port, that overlapping recovery starts could split ownership, and that a partial startup lock could survive failed publication; inspection now fails closed, recovery keeps exact cross-origin links, and an ownership-checked startup lock serializes the pair and removes its own partial files. A route probe also caught Studio document deep links falling through to Archive, so the host now rewrites HTML navigation under `/studio/` to the Studio entry. The canonical multi-CLI review was attempted, but Claude's user settings failed validation before its expired OAuth token stopped the retry, while Codex could not reach its model service and the external escalation was denied; two independent non-author critics completed the required harsh review and reported no remaining findings. The topology moved from two listeners and two browser origins to one listener and one origin, while D-033 explicitly preserves the build, database, backup, and publication boundaries that matter.

## Local startup became idempotent without rotating browser data origins

**Timestamp:** 2026-08-23

**Action:** Replaced unconditional strict-port child spawning with a Node/Vite launcher that identifies existing Badge applications, starts only a missing companion, selects an adjacent free pair on first conflict, exclusively remembers that pair outside Git, and drives cross-links from the active pair.

**Result:** Repeating `npm start` now succeeds by reporting the already-running Archive and Studio. A first launch whose preferred ports belong to other software moves to a free pair automatically; later starts reuse that pair and refuse unrelated occupation rather than making IndexedDB data appear lost.

**Validation:** Contract tests cover complete and partial reuse, first-selection fallback, unresolved-listener refusal, slow and wrong-role Badge identity, the exact markers in both shipped entry points, every adjacent-start parity, reserved-port skipping and record refusal, established-origin refusal, same-pair and competing-pair publication, read-during-publication invisibility, interrupted-publication cleanup, malformed-record preservation, terminal stop-listener cleanup, and preferred, development, arbitrary, and maximum companion links. A headless Chromium control occupied `4173` and `4174`, launched the apps at `4180` and `4181`, loaded both surfaces, traversed Archive to Studio and back on the selected pair, and emitted no console errors. Windows PTY controls launched the exact `npm start` command, observed a second launcher reuse it, sent one `Ctrl+C`, confirmed the launcher process exited, and found both listeners released. The final repository gate passed `263` tests across `44` files, both production builds, typecheck, lint, boundaries, documentation, formatting, and a zero-vulnerability dependency audit.

**Notes:** The reported listeners were Badge's own Archive and Studio processes. Picking unrelated ports on every invocation would have hidden origin-scoped Archive memories and Studio drafts, so the dynamic choice is intentionally one-time and machine-local; deliberate relocation remains a backup or migration operation. Independent review caught and gated a competing-first-writer overwrite, a partial final record during the first correction, a slow-marker misclassification, an odd-start allocator gap, an unbound HTML-marker test, reserved-port record acceptance, an unreliable ConPTY control-byte path, and the maximum-port companion boundary before delivery. The canonical multi-CLI retry again produced no review artifact: Claude returned an expired-OAuth `401`, and Codex remained silent until bounded termination; the independent non-author critic instead completed the current-tree adversarial and lifecycle review with no remaining P0, P1, or P2 finding.

## The documentation target became a runnable Archive and Studio foundation

**Timestamp:** 2026-08-23

**Action:** Scaffolded the Node 24 npm workspace and implemented separate React and Vite Archive and Badge Studio applications, strict render and pack contracts, independent browser-local repositories, deterministic catalogue fixtures, pack compilation and structural admission, and a shared React Three Fiber renderer candidate with a renderer-independent fallback.

**Result:** Archive can browse four already-published visual fixtures, propose or directly author a one-line saying, atomically activate a dated private memory with its exact recipe and source bytes, replay the keyboard-safe ceremony, reload it, export a self-contained earned-memory backup, restore it monotonically into a clean profile, preserve historical earned semantics only within the same qualified identity, and repair damaged source art without replacing readable personal state. Studio can compare three distinct Yosemite candidates, preserve an uploaded original by hash, create a separate local treatment, persist selection and appearance across reload, manipulate shape, material, border, object, camera, zoom, and light, normalize the chosen pixels to canonical PNG, synchronously exclude competing work during publication, and offer a frozen independently admitted targeted pack together with its exact independently admitted and template-compatible theme dependency.

**Reasoning:** The smallest useful slice had to prove the Archive–Studio boundary and data integrity at the same time as the visual direction; a beautiful façade over shared storage, unresolved art, or a flattened badge would contradict the owner's core requirements.

**Validation:** The final automated suite passed 248 tests across 44 files and covered strict contracts, hostile pack admission, deterministic compilation, exact starter catalogue and theme bytes, closed-object and exact-template dependency closure, conservative aggregate decoded-memory limits, archive transitions and IndexedDB transactions, qualified earned-identity restore refusal, blocked and terminated connection retry, structured-clone compare-and-swap, bounded full PNG decoding and checksums, offline deterministic WebP-to-PNG fixture derivation with exact pixel equivalence, concurrent isolated fixture generation and public-directory repair, the complete starter-source startup budget, state-preserving corrupt referenced-source repair, exact safety-checkpoint restore and replacement, state-only rescue evidence, stale replacement reclassification, exact Blob restoration, pre-allocation backup refusal, bounded pre-decode Studio image inspection, publication input snapshots and operation exclusion, multi-identity Studio assets, legacy ambiguity, prospective derivation-graph validation, and viewer-state bounds. Headless Chromium exercised two strict origins, candidate selection, identical generated-and-uploaded source lineage, non-destructive processing, reload and deduplication, recipe persistence, frozen exact-byte pack-and-theme download, saying regeneration and direct writing, committed activation, focus containment and return, clean-profile backup restore, corrupt-profile quarantine recovery, missing-historical-art rescue, restore compare-and-swap, forced GPU fallback, reduced motion, and front, oblique, edge, and back views at 1440×1000 and 1100×900.

**Code reviewer comments:** Independent package critics found mutable admitted bytes, shallow and mislabeled image validation, unchecked PNG expansion and checksums, incomplete ZIP header checks, incomplete backup reference closure, destructive restore semantics, same-ID catalogue poisoning, earned identity rebinding, fixed-gallery restore incompatibility, unreachable and state-replacing source recovery, unsound structured-clone recovery comparison, poisoned or blocked IndexedDB opens, unsafely shared fixture output, claimed rather than compiled theme identity, missing and template-incomplete dependency closure, unreferenced private-object hitchhiking, aggregate decoded-memory amplification, upload decode before dimension refusal, upload-versus-publication races, publication input mutation races, lifecycle handling gaps, post-commit observer rejection, unverifiable one-step safety download, and visual pins that did not freeze the full recipe; each in-slice issue was corrected and regression-tested. Complete installed-pack ledgers and arbitrary unearned installed objects in Archive backup, Studio project backup and durable prepared-release history, Archive pack installation, and the target same-version install policy remain explicitly outside this foundation rather than being falsely claimed.

**Recovery reviewer controls:** Review exposed that Vite could hide a broken plain-Node TypeScript barrel, startup compatibility classification could conceal a damaged earned source, a safety download did not originally bind the final write to its exact snapshot, an incompatible record with unavailable non-starter art could make the promised self-contained handoff impossible, and a stale tab could retain replacement authority after another tab made the Archive compatible. Direct native-Node import, source-first startup audit, exact export-to-write compare-and-swap, explicitly non-restorable state-only rescue evidence, and fresh safety-checkpoint reclassification now gate those classes. A compatible healthy snapshot returns to full-backup normal monotonic restore, a compatible damaged snapshot returns to repair, and only an exact incompatible snapshot with unrepairable art can reach state rescue. The privacy control staged a synthetic `.badgeevidence.json` in an isolated repository and proved the delivery gate rejects it.

**Studio identity reviewer controls:** Content-only candidate deduplication originally collapsed a byte-identical fixture and upload, lost provenance across reload, and produced duplicate React keys; a first one-lineage derivative repair then rejected byte-identical outputs from different parents, while permissive version-1 matching could relabel legacy provenance. Candidate identity is now separate from content identity, original and derivative rows carry bounded admitted identities or exact lineage pairs, version-1 ambiguity restores to an inert null selection, legacy derivatives remain preserved but non-publishable, and every prospective transaction graph is cycle-checked before mutation. Contracts cover fixture migration, fabricated generated identity, two-provenance identical treatment, lineage tampering, raw version-1 derivative plus version-2 draft, and root-to-descendant output returning ancestor bytes without mutation.

**Renderer reviewer controls:** Independent renderer review reproduced a capability-dependent crop change for non-square art, React's passive-wheel warning plus engaged page scroll, twenty Canvas removal/addition cycles during a twenty-step border-width edit, inspection-pose resets during ordinary appearance comparison, a rectangle fallback whose `320.61 × 221.22` front was paired with a `16 × 138.59` edge, and a forced-fallback first run that still probed WebGL2. The corrections now share one shape-aware source rectangle across live and fallback, give the viewer an exact non-passive wheel listener, separate source-session and explicit-recovery identities from recipe content, preserve Canvas, texture framing, pose, light, and fallback view across recipe edits, size all fallback views through one object frame, and short-circuit the forced capability probe. Targeted renderer contracts cover each control; final whole-repository and browser gates remain the delivery gate rather than being inferred from these focused checks.

**Final browser controls:** The wheel control measured no scroll while disengaged or engaged and `275 px` after release while zoom changed from `100%` to `91%`; the live lifecycle control preserved the same Canvas and WebGL context, `110%` zoom, light mode, and engagement through recipe edits; forced fallback invoked zero WebGL2 probes, mounted no Canvas, preserved the back view, and kept equal physical height for circle, square, rectangle, and shield. Recovery controls completed readable incompatible replacement, combined damaged-source repair then replacement, exact restore compare-and-swap refusal after a second-tab mutation, and arbitrary missing historical art through explicitly non-restorable rescue while preserving an earned activation. The final Studio lineage control progressed from three candidates to six distinct generated, uploaded, and processed identities, restored all six with uploaded processed selection and both stored provenance lineages, repeated at six, emitted no duplicate-key or console errors, and the final `1100 × 900` sweep showed the complete polished workbench without clipping or overlap.

**Notes:** Browser verification caught integration defects that unit tests did not: Node 24 on Windows rejected the `.cmd` child-process launch, so the runner now starts hidden Vite children directly; Chromium WebP encoding added a forbidden ICC profile, so Studio publication uses metadata-free PNG; the activation dialog returned focus to an unmounted submit button until it targeted the earned-state replay control; published Studio controls remained editable and regenerated bytes behind an “offer again” label until the emitted release became a locked exact-byte state; the forced-fallback status pill overlapped its notice until the fallback layout reserved that space; and 64-byte streaming decoder input left Archive on its opening screen for 24,516 ms until a still-bounded 64 KiB input reduced the same measured reload to 1,352 ms without changing any integrity check. The image-integrity problem escalated after a hash-only check and then a structural check both accepted recomputed corruption: the fixed benchmark showed tolerant browser and local WebP decoders could still return pixels from malformed bytes, while canonical tracked PNGs violated the Git blob ceiling or visibly lost detail. The resulting route keeps compact WebP only as verified developer input and deterministically derives lossless strict PNGs into ignored build output, which preserves Archive's fully decoded durable boundary without shipping a decoder. A mid-run exact-pin schema hardening correctly preserved and refused synthetic pre-change test data; no released user format or user data existed, and only isolated task browser profiles were cleared for the final sweep. Plain Node then exposed that Vite and Vitest had hidden the contract's source-resolution behavior; after two failed import variants triggered a deliberately different route search, the chosen native Node path used explicit `.ts` imports and erased the sole parameter property rather than adding a duplicate validator, experimental runner, or temporary compilation lifecycle. The full gate also caught a transient Windows `EPERM` while atomically renaming a task-owned fixture lock directory; a deterministic fault-injection control now proves bounded retry closes that OS race without weakening link or ownership checks. The final Studio browser sweep proved a real thick oblique edge, independent light movement, frozen controls, and exact paired downloads whose theme digest matched the pinned `PackRef`. The canonical external review lanes were attempted but unavailable because Claude OAuth had expired and sandboxed Codex could not validate the machine certificate; escalation to transmit the working tree was correctly refused, so independent non-author subagents supplied the required persistence, contract, visual, and final-integration reviews instead.
