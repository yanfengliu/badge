# Devlog — 2026-08-28

## A frozen edition no longer freezes the image-upload door

**Timestamp:** 2026-08-28 10:00–12:30 PDT

**Action:** Kept the Studio own-image action enabled before and after publication freeze, made a validated post-freeze upload begin an editable replacement-edition draft, and retained every displaced in-session release for exact pack-and-theme re-offer.

**Result:** The same visible action now accepts PNG, JPEG, or WebP source art before or after an Archive activation without opening or inferring Archive lifecycle state. Invalid or canceled input leaves a frozen release untouched, changed publication creates a content-addressed replacement, unchanged final content re-offers the same release without duplicate history, and the activated Archive record remains pinned to its sealed visual and personal fields.

**Reasoning:** Studio and Archive own separate private databases, so Studio cannot truthfully branch on whether a record is planned or earned. The safe UI operation is lifecycle-agnostic authoring: select and construct a new Studio source, preserve every exact release, and leave any later Archive application to an explicit validated install, rebind, or reviewed migration workflow.

**Validation:** Four focused Studio and compact-layout files pass `20` tests, including cancel, invalid upload, valid replacement, exact old-byte re-offer before and after replacement publication, unchanged-publication idempotency, changed-pixel versioning, and `A → B → A` release-history uniqueness. The final repository suite passes `171` files with `1,011` tests passing and one skipped; typecheck, lint, formatting, boundaries, build, local startup, and audit pass. Headless Chromium inspected the initial, frozen, replacement, invalid-input, reduced-motion fallback, `1440 × 1000`, `390 × 844`, `320 × 700`, and `844 × 390` states. At the `320px` floor, the real-length prior-release control measured `292px × 44px`, the document kept `clientWidth === scrollWidth === 320`, and the shortened visible digest retained the complete version and digest in its accessible name and tooltip. A separate clean profile activated Yosemite, showed one collected record, opened Studio, selected the manufactured _Sapiens_ upload, returned to Collection, and replayed Yosemite with its original cloisonne description, date, and quotation unchanged. Browser verification produced no console errors; its only warning was the existing Three.js `Clock` deprecation.

**Code reviewer comments:** Independent harsh review rejected the first evidence wording because it proved publication rather than a real Archive activation, found that replacement publication discarded the prior release, and found unconditional new-version copy misleading for unchanged deterministic content. A final pass then found duplicate release history on `A → B → A` and a production-length version label that could overflow the phone floor. The final implementation adds exact unique in-session prior-release history, idempotent unchanged-publication copy and tests, a compact accessible retained-release label, and a real activation-to-Studio-to-replay browser journey while keeping the claim narrow: Studio creates a replacement draft but does not apply it to Archive. The reviewer reran the corrected state, mounted UI, and mobile contract tests and found both final issues resolved with no introduced blocker. The reviewer also noted that the current uploaded-art accessibility text is a neutral provenance description rather than a user-authored visual description; a durable per-candidate description field would require a separately reviewed Studio draft-schema change and remains outside this UI slice.

**External review availability:** The pinned read-only Codex lane reached `gpt-5.6-sol` at ultra effort but the sandbox certificate chain could not connect, and host-network escalation was denied because it would export private source context without explicit user approval. The pinned Claude lane could not authenticate because its OAuth token had expired. Neither unavailable lane is counted as a pass or replaced by a workaround.

**Notes:** Applying the replacement edition to an Archive record remains a separate explicit install, rebind, or reviewed-migration workflow and is not implemented here. The current release and prior-release list are intentionally ephemeral fixture UI state rather than the target durable prepared-release ledger.
