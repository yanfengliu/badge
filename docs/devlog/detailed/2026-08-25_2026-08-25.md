# Devlog — 2026-08-25

## The fifty-state source-study edition reached Discover

**Timestamp:** 2026-08-25

**Action:** Added one Census-FIPS-bound authoring record for each of the fifty U.S. states, one three-direction campaign per state, one visually selected `896 × 896` source study and deterministic `128 × 128` derivative per state, and one safe production discovery projection per state. The complete campaign uses every one of the existing twenty-four immutable art styles, limits a primary style to at most three assignments, and records the exact canonical primary prompt, selected bytes, derivative bytes, accessible description, normalization recipe, content origin, and source-association hashes.

**Result:** Discover now contains four available starters, sixty-two additional national-park studies after Yosemite deduplication, and fifty U.S. state studies: `116` concepts across U.S. National Parks, U.S. States, Books Read, and Life Milestones. State studies are explicitly `Potential` and `Not yet published`; they do not enter Collection, activation, Timeline, quotation generation, personal state, or pack authority. Every state design is visible as compact greyscale potential art, while the full selected source remains in Studio-owned authoring assets.

**Quotation standard:** Every state record requires a designated quotation ID from the closed historical-quotation bank. The exact text, named historical figure, supporting source URL, source title, and optional English-Wikipedia biography are frozen by contract. Current sources are Mark Twain's _The Innocents Abroad_ at Project Gutenberg, John Muir quotations at the National Park Service, and Henry David Thoreau's _Walking_ at Wikisource; the quotation source remains distinct from the optional biography link.

**Art generation:** The fifty source studies were generated through the built-in image-generation workflow from the exact primary canonical prompt for each state, with no uploaded reference image and no typography or physical-badge construction in the requested output. Native inspection rejected a signature-like Massachusetts result and replaced it. The first New York result also falsely turned Niagara into a mountain wilderness; the brief gained a New York State Parks factual source plus an explicit flat Great Lakes horizon and no-mountains exclusion, and the regenerated mineral-line engraving depicts the low American and Bridal Veil falls, Luna Island, and an urban flat horizon.

**Validation:** Exact tests freeze the fifty Census identities, FIPS-derived IDs, three unique styles per state, all twenty-four assigned styles, four complete historical quotations, required quotation membership, exact prompt hashes, exact source and derivative hashes, byte ceilings, and header dimensions. A separate System.Drawing decoder gate opens all fifty state and sixty-three park sources, materializes bitmaps, probes pixels, and rejects a deliberately truncated twenty-one-byte `896 × 896` SOF header; derivative generation and the production manifest similarly fully decode and key-bind all `113` thumbnails totaling `682,741` bytes.

## Collection and Discover scaled without turning into a dashboard

**Timestamp:** 2026-08-25

**Action:** Collection now renders only sets represented by earned badges, including a single quiet cabinet-waiting state when nothing has been collected. Discover reveals twenty-four cards at a time, resets that reveal when set or search context changes, gives exact USPS aliases precedence over substring matches, and uses five flexible set segments that become a balanced three-plus-two arrangement before collapsing to one column.

**Result:** A large catalogue no longer mounts every image at once or fills Collection with empty shelves. `WA`, `CA`, and `OR` resolve directly to Washington, California, and Oregon; the state set progresses from twenty-four to forty-eight to fifty visible cards; changing the query returns to the first twenty-four; and the set selector has no fake empty cell. Existing Collection shelves remain closed by default and preserve the full-segment hover and focus browse treatment.

**Production media boundary:** The Archive build emits every derivative beneath `assets/discovery/<catalogue>/<slug>-<hash>.jpg`. The postbuild gate reconstructs the qualified source key from that path and compares the emitted bytes, SHA-256 digest, byte count, and dimensions with the exact manifest entry. The runtime resolver test separately binds all `113` keys to their exact `/assets/<catalogue>/thumbnails/<file>` source URL. A control that exchanged two built JPEG payloads failed on the first mismatched catalogue key, closing both resolver rotation and emitted-payload swap classes.

## Later catalogue families stayed truthful

**Timestamp:** 2026-08-25

**Action:** Added versioned planning-only records for the owner's remaining expansion directions rather than pretending thousands of badges had already been illustrated or published. The plans record Washington's `281` municipality snapshot, California's `483` incorporated-city snapshot, the PBS Great American Read's `100` books, GO TOKYO's current `34`-attraction editorial snapshot, one generic owner-entered Michelin restaurant milestone, and `64` optional life-milestone prompts.

**Result:** Production cannot import the planning modules. City, book, Tokyo, Michelin, and optional milestone records carry no generated art, selected source, pack reference, activation, or personal completion. Michelin remains a generic user-entered achievement until a separately reviewed licensed or terms-compatible data source exists; the app does not scrape or represent a Michelin directory.

## The visual direction became a durable record

**Timestamp:** 2026-08-25

**Action:** Promoted current Archive Collection and Discover reference captures into `docs/design/assets`, updated the asset index and visual direction with exact route, viewport, fixture, current-versus-aspirational, set-count, progressive-reveal, compact-layout, and interaction semantics, and added a standing local rule that future material UI changes refresh the record and both captures.

**Visual verification:** Headless inspection covered the earned Collection cabinet and state Discover surface at `1440 × 1000`, the flexible selector at `900` pixels, and the one-column compact form at `640` pixels, with no horizontal overflow or broken state images. The final fifty-state contact sheet was rebuilt after the corrected New York source and inspected as one whole-edition diversity sweep; every selected source had also been inspected at native resolution, because the contact sheet is a coverage instrument rather than proof of prompt conformance.

**Review closure:** Independent harsh review caught an empty-Collection duplicate message, weak source-integrity claims, substring-only postal search, reveal state that lacked a mounted reset proof, a fake selector cell, over-required Wikipedia data, an optional state quotation, a fantasy New York landscape, stale measured bytes, an unordered output-hash check, resolver values that could rotate, and header-only JPEGs that could masquerade as images. Each received a contract, visual correction, or decoder control before release. The final reviewer found no remaining P1 or P2 defect after the key-qualified production output, exact resolver binding, full source decode gate, and session history landed.

**Final gates:** Node `24.12.0` ran `npm run verify` successfully with `116` test files, `703` passing tests and one intentional skip, production builds for Archive, Studio, and the combined local site, task-owned local startup, boundary, documentation, formatting, and high-severity dependency-audit checks. Every touched product and script source remains below the repository's 500-line limit.

## Archive and Badge Studio became phone-capable

**Timestamp:** 2026-08-25

**Action:** Added a final responsive layer to each independently built application, edge-to-edge viewport metadata to all standalone and combined-site entry documents, safe-area and dynamic-viewport handling, a compact four-destination phone header, bounded dialogs and viewers, stacked detail and form layouts, deliberate touch rails for ordered dense content, and a `320px` document-width floor with no page-level horizontal overflow. Archive phone behavior covers Collection, Timeline, Discover, preparation, notices, restore, disclosure, activation ceremony, and memory replay; Studio covers candidate selection, uploads and reprocessing, live construction, appearance controls, publishing, and the expandable art-direction library.

**Touch ownership:** The live viewer previously changed to `touch-action: none` as soon as contact engaged it, so a tall canvas could trap the most natural phone page-scroll gesture. Touch now begins without pointer capture, yields once vertical intent dominates, captures only after horizontal intent passes an eight-pixel threshold, and disengages on up, cancel, or lost capture. Mouse capture, persistent mouse engagement, keyboard inspection, renderer-neutral state changes, passive replay, and fallback behavior remain unchanged.

**Contract evidence:** A cross-application CSSOM test failed first on missing `viewport-fit=cover` metadata and absent responsive layers, then froze final-import ordering, zero root minimum width, clipped document overflow, safe-area variables, `100dvh`, equal four-column phone navigation, deliberate internal rails, ordinary and conditional `44px` action floors, `16px` form text, symmetric Studio library safe edges, naturally flowing ceremony controls, and aspect-preserving live plus fallback stages in portrait and short landscape. A mounted Studio test enters and clears the conditional source-style override. Shared renderer tests separately freeze vertical touch release, horizontal capture and rotation, post-capture pitch, release and cancellation cleanup, browser-owned `pan-y pinch-zoom`, unchanged mouse and keyboard behavior, and the shape-derived stage-fit length for circle, square, rectangle, and shield. A promoted-asset gate fully decodes both mobile references as strict `390 × 844` PNGs after checking magic, CRCs, bounded DEFLATE output, and scanline filters.

**Visual verification:** Headless Chromium inspected Archive and Studio at `320 × 568`, Archive at `430 × 932`, Discover at `390 × 844`, and both applications at `844 × 390`. Each inspected document reported equal client and scroll width; the four destinations remained visible; Discover sets, Studio candidates, and collected-artifact rows were the only intentional horizontal rails; phone controls and inputs met their measured floors; and exact `390 × 844` Archive Discover and Studio candidate-selection PNG captures became canonical current-implementation references. A separate isolated profile activated Yosemite and inspected the complete ceremony, populated Collection cabinet, live passive replay, forced replay fallback, and forced activation fallback in phone portrait and short landscape. The first fallback replay exposed a width-sized static badge spilling into the details; its final size-query formula uses one definite axis and a shape-derived height-fit length. A dedicated harness rendered circle, square, rectangle, and shield in both ceremony and replay at `320 × 568` and `844 × 390`: all sixteen frames stayed contained, all stage client and scroll heights matched, and worst aspect-ratio error was `0.000143`. A protocol-level touch swipe over Studio's live viewer moved page scroll from `733` to `1918` while computed touch action remained `pan-y pinch-zoom`; simulated `37px` left and `41px` right insets reached both art-library panes without overflow; the conditional restore target measured `44px`; and the complete flow logged no browser errors.

**Review closure:** Independent review found short-landscape target and safe-area gaps, fixed-size activation children, the conditional restore action below the touch floor, asymmetric Studio library insets, and a stale ceremony-height record. Each became a selector-specific contract, mounted state check, responsive correction, or refreshed visual record before the final evidence run. The forced-fallback sweep then found a static-art spill; the next review rejected a circle-only containment proof because a two-axis cap could squash rectangles, leading to the all-shape ratio harness and one-axis fit formula. That same review decoded the earlier mobile files as JPEG streams behind PNG names, so both references were recaptured as exact PNGs and gained a full-decode gate.

**Final gates:** Node `24.12.0` ran `npm run verify` successfully with `119` test files, `716` passing tests and one intentional skip, production builds for Archive, Studio, and the combined local site, isolated local-startup verification, boundary and documentation checks, complete formatting, and a zero-high-severity dependency audit. The first complete run reached formatting after all earlier gates and correctly rejected the newly strengthened CSS contract file; formatting that file and rerunning the complete command passed. After review strengthened the conditional-control, safe-area, ceremony, replay, fallback-shape, and promoted-asset contracts, the complete command passed again.

## Discover badge cards became the interaction surface

**Timestamp:** 2026-08-25

**Action:** Replaced each published card's small CTA with one stretched native button spanning the complete Discover card. Collected cards still open the exact earned replay and published unearned cards still open preparation inside Discover. Source-study cards now open a focus-contained read-only preview that repeats only the admitted `128 × 128` derivative, title, criterion, status, and set membership, explicitly names the missing runtime capabilities, and creates no record, quotation, renderer recipe, installation, or persistence write.

**Contract evidence:** Component contracts require one card action per rendered result, exact destination-specific accessible names, full-surface positioning, hover and keyboard focus treatment, and no duplicate tab stop. Mounted interactions prove preparation and replay remain distinct; the source-study preview receives initial Close focus, contains Tab, makes the background inert, closes through Escape or Close, and returns focus to the originating card; and a twenty-fifth published card remains rendered and regains focus after preparation temporarily replaces Discover. The mounted real App replays a collected badge from Discover and returns focus to that full-card action, preserves forty-eight revealed cards across a preparation round trip, and retains a filtered query across the same transition, while the mobile CSSOM contract freezes the stretched action and phone-bounded overlay.

**Visual verification:** The task-owned browser clicked directly on artwork rather than CTA copy at `1440 × 1000` and `844 × 390`; hit testing resolved the artwork point to `.discovery-card__action`, source studies opened the preview, and published Yosemite opened preparation and returned to its exact card. The hover state measured the rust border, pointer cursor, and two-pixel lift. At `320 × 568`, every initial result exposed an action at least `137px` tall, the preview stayed within a `296 × 544px` internally scrolling card, focus moved to Close, and document overflow remained zero. At `844 × 390`, the complete preview fit in an `824 × 351px` card with no internal or document overflow. The browser logged no application errors.

**Review closure:** Independent harsh review found no P1 and confirmed that source-study previewing did not leak runtime authority or overflow the checked layouts. It caught one P2: preparation unmounted Discover's locally owned reveal count, so a published card beyond the first twenty-four could disappear before exact focus restoration. The reveal count now belongs to the Archive view session, resets for a new search, set, replay-set browse, location entry, or primary Discover entry, and persists only across preparation and Back. The same review noted that the mounted preview evidence had not directly exercised Escape, Tab containment, or inert isolation; those paths now have explicit assertions. Follow-up review independently ran the focused interactions and reported no remaining P1 or P2 finding.

**Final gates:** Node `24.12.0` ran `npm run verify` successfully with `119` passing test files, `720` passing tests and one intentional skip, production builds for Archive, Studio, and the combined local site, isolated local-startup verification, boundary and documentation checks, complete formatting, and a zero-high-severity dependency audit.

## Badge Studio became the fourth root-document section

**Timestamp:** 2026-08-25

**Action:** Replaced the route-split local host with one canonical `/` document that composes Collection, Timeline, Discover, and Badge Studio as peer primary sections. Badge Studio now uses `/#studio`, direct hashes and back or forward navigation remain meaningful, and legacy `/studio` HTML requests redirect to the Studio hash instead of mounting a second document.

**Result:** The host owns location, focus transfer, and document theme. Leaving Studio is blocked while a Studio operation is busy and otherwise waits for the exact current draft to flush before the section changes; surface CSS remains scoped so the composed document does not let Studio global rules restyle Archive.

**Boundary and startup:** The host composer is the sole production importer of both surfaces. Archive and Studio retain separate IndexedDB names, repository interfaces and adapters, Blob ownership, backup formats, and standalone isolation builds; neither surface imports the other or opens its database. The root document has one CSP and makes no route-specific service-worker claim. Startup requires the versioned unified marker, one root mount, and one supported same-origin live or built app module at `/` plus the canonical `308` redirect from `/studio`, refuses marker-only, missing-root, broken-entry, external-entry, hybrid, Archive-only, and Studio-only identities, and keeps the existing remembered origin because the path change does not relocate origin-scoped IndexedDB data.

**Contract evidence:** `apps/host-web/src/host-location.test.ts` freezes root-hash parsing and history writes, `apps/host-web/src/App.interaction.test.tsx` exercises host composition and guarded section changes, `apps/studio-web/src/studio-leave-guard.test.ts` freezes busy blocking and exact-draft flushing, `apps/host-web/host-contract.test.ts` constrains legacy-route redirection, the sole cross-surface composer, CSS ownership, and separate database names, `scripts/local-launcher.test.mjs` freezes the versioned root and mount, live and built app-entry forms, usable entry response, canonical redirect, and refusal of marker-only, broken, external, multiple-entry, hybrid, and legacy identities, and `scripts/check-host-build.mjs` checks the one-document production output while the Archive and Studio standalone builds remain available for isolation verification.

**Review notes:** The first host draft conditionally unmounted Archive and lost an unfinished preparation, treated rejected browser traversal with `replaceState` and could destroy the destination being restored, and later restarted indexed history at zero when an unknown entry was encountered. The repaired host retains visited surface state, disconnects hidden Archive effects with React Activity, restores rejected indexed traversal with the exact inverse history delta, and preserves unknown entries as unindexed pushes. The first Studio leave guard also let a queued autosave race the final draft, could permit a second exit after a failed save, and later blocked a clean already-saved draft forever after storage loss; writes are now coalesced and serialized, the exact current draft is the final write, failed writes latch refusal, and clean drafts may leave after a later connection loss. Review also caught document-global Studio CSS leaking into Archive, the Archive root's `320px` minimum creating a `15px` scrollbar gutter at the exact phone boundary, and launcher reuse trusting a marker-only or hybrid stale site; surface datasets now scope both themes, the Archive root contracts to the available width, and launcher identity now proves the current entry and redirect topology before reuse. The first completeness repair then counted any built `/assets/index-*` module as Badge evidence and misclassified the real unrelated AoE2 Vite listener on `4173`; Badge evidence is now marker-only, while entry, mount, and redirect checks determine completeness only after that identity is established.

**Validation:** A grounded independent critic found and rechecked each history, lifecycle, focus, effect, and CSS repair and reported no remaining P1 or P2 finding. The external multi-CLI lane could not run because the environment did not authorize repository egress, so it was not counted as review evidence. Real Chromium verified root-to-Studio-to-root clicks, direct `/#studio`, legacy `/studio` canonicalization, Back and Forward, modal focus and body-lock disconnect and reconnect, same-section Archive draft retention, exact last-moment Studio draft persistence, one exposed non-inert surface, correct `html` and `body` themes, zero console errors, and layouts at `1440x1000`, `320x568`, and `844x390`; the only console output was the renderer dependency's existing `THREE.Clock` deprecation warning.

**Final gates:** Node `24.12.0` ran `npm run verify` successfully with `122` passing test files, `771` passing tests and one intentional skip, production builds for Archive, Studio, and the single-root local site, isolated one-listener startup verification, boundary and documentation checks, complete formatting, and a zero-high-severity dependency audit.
