# Devlog — 2026-08-28

## Opening Collection stopped downloading the whole application's future and past

**Timestamp:** 2026-08-28 10:24–12:11 PDT

**Action:** Split the root host into route-selected Archive and Studio chunks, moved the live 3D viewer behind an Archive-owned lazy boundary while keeping static recipe previews in a small export, added warm Archive and dark Studio loading shells with actionable chunk-failure recovery, and made startup fetch historical starter art only for earned alpha.3 activation pins. Explicit recovery still assembles the broader published-or-pinned closure, but it now reads fresh persisted state when possible and retains newly fetched bytes only after repository validation succeeds.

**Result:** The production application moved from one `2,998,951`-byte raw / `647,968`-byte Brotli JavaScript entry to `200,687` bytes of Brotli Archive-startup JavaScript plus CSS; the `924,703`-byte raw live renderer and complete Studio surface are deferred. Normal startup image traffic moved from eight PNGs totaling `7,996,418` bytes to four current PNGs totaling `1,044,652` bytes, for a final budgeted startup transfer of `1,245,339` bytes. On a cache-disabled `8 Mbps`/`100ms` profile with existing IndexedDB, usable Collection moved `11.460s → 2.825s`, while first contentful paint measured `340ms` in the measured final-browser run.

**Reasoning:** Alpha.3 repair art had been treated as universal startup input because it can reconstruct historical bytes, but only an earned activation keeps that historical visual; an unearned exact alpha.3 record is atomically upgraded from current sources and does not need the old file. The root's static import topology likewise made a one-document product look like a one-bundle requirement, although D-050 requires shared navigation and isolated application authority rather than eager evaluation. Manifest traversal and state-derived source selection preserve those boundaries without relying on filename coincidence or weakening migration checks.

**Validation:** Focused startup, asset-selection, restore-state, recovery-transaction, lazy-presentation, rejected-chunk, loading-contrast, host, and mounted saying-flow contracts pass. The production manifest gate proves separate dynamic surfaces, complete-closure markers, deferred Studio and live renderer, a `96 KiB` Brotli shell-code ceiling, a `280 KiB` Archive-startup-code ceiling, a `1,080 KiB` exact-current-art ceiling, and a `1,400 KiB` combined ceiling; the passing values are `54,627`, `200,687`, `1,044,652`, and `1,245,339` bytes respectively. Headless Chromium proved exactly three initial scripts and four current PNGs, zero legacy PNGs, on-demand renderer and Studio chunks, live rotation/light/zoom controls, centered loading and actionable failure shells, and no compact overflow at `320 × 568` or `844 × 390`; the deferred renderer's loading and rejected-chunk states both measured the intended `220px` short-landscape stage with exact `844px` client and scroll widths. The only normal-flow console warning was Three.js's existing `Clock` deprecation.

**Repository gates:** The final full suite passes `171/171` files with `1,010` tests passing and one intentional skip; lint, repository-wide format, the production build and byte budgets, boundaries, audit, and local-startup checks pass. The root typecheck's sole diagnostic is in the concurrent uncommitted Studio replacement file `studio-release-state.ts`; an Archive dependency-graph compilation and an exact-diagnostic-controlled root run confirm no TypeScript error in the loading unit. `check:docs` reports only the pre-existing seeded fleet-constitution drift that this unmanaged repository explicitly forbids editing locally.

**Code reviewer comments:** The first two-phase draft cached fetched current and legacy bytes before repository hash validation, which meant one corrupt response could poison every later recovery attempt. Independent review moved each cache publication after successful admission and made recovery additions attempt-local. A second pass caught that the first selector still fetched unearned alpha.3 published sources and that its activation-pin test constructed a domain-invalid record; separate startup and recovery selectors now gate zero legacy requests for upgrade-only state and exact earned-pin requests for valid history. The same pass caught React state being preferred over a fresh repository read during recovery, which could omit another tab's newer source pin; the fresh read now wins and a focused contract freezes the fallback behavior. Final harsh review then caught missing lazy-import error boundaries, dropped viewer layout classes, sub-AA Studio loading-label contrast, a JavaScript-only performance ceiling that omitted the dominant art, and untested recovery assembly; each now has a focused contract, and the recovery transaction exposes no new session assets unless repository recovery succeeds.

**External review availability:** The pinned Codex lane reached the correct model and ultra effort but the sandbox certificate chain could not connect; host-network escalation was denied, so no source payload left through that path. The Claude lane was not used as a workaround for the same denied export boundary, and neither unavailable lane is counted as a pass.
