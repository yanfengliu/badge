# Devlog — 2026-08-24

## Wool material stopped resolving into a triangle lattice

**Timestamp:** 2026-08-24

**Action:** Replaced the wool material's hard short-period checker carrier with the versioned `badge-wool-fibers-v2` field of deterministic irregular paired microfibers, reduced its repeat from five to three, lowered the front, body, and back bump strengths, and applied the renderer's anisotropy with an eight-times cap.

**Result:** Wool still reads as a soft stitched artifact under ordinary and grazing light, but the repeated chevrons no longer cross the source artwork or border. Metal and enamel behavior, source pixels, recipes, crop, geometry, viewer state, and controls are unchanged.

**Validation:** A contract-first red control preserves the old carrier and proves the instrument detects its `0.9919` maximum absolute short-period correlation. The final deterministic texture peaks at `0.0546` across every two-dimensional offset from two through sixteen pixels, below a visually calibrated `0.15` ceiling; it retains a bounded `62`-level tonal range and more than twenty-four distinct values, has no exceptional repeat seam, and configures repeat wrapping, mipmaps, trilinear minification, update state, exact material identity, and bounded anisotropy for unavailable through excessive device reports. An independent geometry probe measured `260` front vertices, `258` indexed triangles, and only the normal `(0, 0, 1)`, proving that the face-wide failure was not mesh faceting.

**Notes:** Missing anisotropy was supplementary rather than causal: moving only the light reproduced the issue without changing camera or texture minification. The old four-pixel over-under step, eight-pixel cosine carriers, diagonal high-frequency term, and aggressive bump strength encoded the visible facets directly. The repair preserves physical response instead of deleting wool relief, and it leaves the separate crease-aware smoothing question for extruded body geometry outside this source-proven defect.

**Visual verification:** Headless Chromium first reproduced the owner-visible chevron field on the real _Sapiens_ rectangle at `133%` inspection scale and a five-degree key light. The fixed Archive render was inspected at `1440 × 1000` through front, oblique, steep oblique, edge, back, normal-light, and the identical grazing-light control, then at `900 × 900` oblique. Badge Studio selected and non-destructively reprocessed a candidate, applied wool, and repeated front, oblique, grazing-light, and narrow edge views. The sweeps retained exactly one Canvas, showed no triangular carrier, and reported zero browser errors; the sole warning remained the pre-existing Three.js `Clock` deprecation.

**Review closure:** Independent harsh review first found that the `0.65` correlation ceiling was too permissive and the repaired texture retained a stale `v1` identity. The final gate lowers the ceiling to `0.15`, extends the scan to two-pixel offsets, proves the instrument against the exact defective carrier, makes anisotropy a required constructor input, and asserts `badge-wool-fibers-v2`. Follow-up review reported no remaining concrete finding and agreed that the separate extruded-edge normal question is causally and visually distinct from the supplied face-wide lattice.

**Final gates:** `npm run verify` passed with 100 test files, 628 passing tests and one intentional skip, production builds for Archive, Studio, and the combined local site, task-owned local startup, boundary, documentation, formatting, and high-severity dependency-audit checks. Every touched source and test file remains below the repository's 500-line target.

## Earned-state replay became one passive badge turn

**Timestamp:** 2026-08-24

**Action:** Added a replay-only `single-turn` presentation to the shared 3D viewer. Earned-state `Replay activation` now waits for the exact live texture, makes one eased vertical-axis revolution over `2,400ms`, and holds the visually identical resting pose without object-versus-light mode, zoom or scale, reset, instructions, pointer, wheel, keyboard, fallback-view, or live-retry controls. First activation, Archive detail, Timeline inspection, and Badge Studio retain the existing interactive viewer.

**Result:** Replay has one ordinary action, Close, while the accepted historical quotation remains visible throughout the turn. Reduced motion and renderer fallback present a stable front. A motion-preference change during the turn cancels its single frame chain and restores the resting pose; turning the preference off begins a fresh complete turn. Automatic graphics-context recovery likewise resets before the replacement canvas becomes visible and restarts only after its texture is ready. The passive Canvas carries the image semantics while loading statuses remain exposed as sibling live regions.

**Validation:** Pure timing contracts prove monotonic yaw from the preset through exact `2π` and a stable hold. Mounted lifecycle tests prove start-after-ready, one cancellable frame chain, no ordinary-rerender restart, unmount cancellation, zero reduced-motion autoplay, mid-turn motion-preference restoration and restart, and an attach-time preference race. A mounted `BadgeViewer` recovery test interrupts the turn, proves the pose is reset before remount with no pending frame, verifies that loading status is outside the ARIA image, and completes the replacement turn. Static and mounted Archive contracts prove replay selects the passive presentation while first activation remains interactive, and passive live or fallback markup contains no inspection, scale, reset, retry, or fallback-view controls.

**Notes:** The first implementation used the shared ceremony's passive presentation for both first activation and replay; harsh review caught that this expanded the owner's replay-only request. The same review found that the card's `680ms` entrance hid the opening portion of the turn, that a runtime reduced-motion change could freeze at an interrupted angle, that a preference change between render and subscription could escape observation, that renderer recovery could reveal one stale-angle frame, that loading statuses were flattened beneath `role="img"`, and that copy incorrectly sequenced the quotation after the turn. The final implementation preserves first-activation inspection, removes entrance animation only for replay, uses `useSyncExternalStore` with attach-time reconciliation, resets before reduced-motion and recovery transitions, and describes the concurrent badge and quotation truthfully.

**Visual verification:** Headless Chromium preserved Inspect object, Adjust light, Zoom, Reset view, and Reset light in first activation at `1440 × 1000`, then sampled replay through front, edge, back, oblique, and returned-front views with one Close control, no application role, and one image. Final and `500ms` hold captures had identical hashes. A real `WEBGL_lose_context` interruption showed the mid-turn edge, a clean `Loading badge artwork…` transition with no stale pose, a front-facing restart, and completion. Reduced-motion replay was inspected at `1440 × 1000` and `900 × 900`; fallback replay was inspected at `1280 × 720` and `900 × 900`. Both fallback widths had one button and zero view switchers, and the static object's bottom remained above the copy boundary at `386.25 < 430.05` and `516.75 < 563.25`. The short fallback sweep first exposed an overlap with the ceremony heading; passive fallback sizing and overflow containment removed it. Browser inspection reported no errors, and all task-owned browser, server, and evidence files were removed.

**Review closure:** Independent audits established the passive replay contract and later challenged scope, visible-turn timing, dynamic reduced motion, fallback wording, accessible naming, and documentation sequencing. Two harsh follow-up rounds then caught the preference-subscription race, renderer-recovery pose, flattened loading statuses, missing multi-viewport reduced and fallback evidence, and the absent detailed history. Each issue received a code or evidence gate before the final re-review.

**Final gates:** `npm run verify` passed with 100 test files, 620 passing tests and one intentional skip, production builds for Archive, Studio, and the combined local site, task-owned local startup, boundary, documentation, formatting, and high-severity dependency-audit checks. Every touched source and test file remains below the repository's 500-line target.

## Studio exact prompts became collapsed disclosures

**Timestamp:** 2026-08-24

**Action:** Replaced the always-expanded exact compiled art prompt in Badge Studio with a native `details` and `summary` disclosure that has no initial `open` state. The summary retains the exact-prompt label and candidate key, while the copy action, focusable prompt text, and live copy status remain inside the expandable body.

**Result:** A selected park or common-achievement direction now ends with one compact `Show prompt` row by default. Pointer click, `Enter`, or `Space` reveals the unchanged compiled prompt and local `Copy prompt` action; the same control can collapse it again without changing candidate role, style, source selection, prompt bytes, or provider behavior.

**Validation:** Static markup tests require native disclosure semantics without an `open` attribute. Mounted interaction tests prove the initial closed state, expansion, re-collapse, exact park alignment, and successful copying only after expansion. The complete suite passes with 95 test files, 610 passing tests, and one intentional skip.

**Notes:** The disclosure is intentionally uncontrolled: it starts collapsed when mounted and preserves the curator's chosen open state while the selected direction changes in place. Switching to a Studio segment without a prompt unmounts it, so returning creates a fresh collapsed disclosure. Copy remains outside `summary` to avoid nested interactive controls, and the candidate key may wrap without displacing the fixed state cue.

**Visual verification:** Headless Chromium inspected collapsed and expanded states at `1440 × 1000` and `900 × 900`, including pointer and keyboard expansion, re-collapse, focus treatment, copy success, prompt scrolling, reduced-motion initialization, and the surrounding candidate, construction, and catalogue surfaces. A whole-Studio sweep also covered front, oblique, edge, and back badge views plus near and far zoom levels. There were no browser errors; the only warning was the existing Three.js `Clock` deprecation.

**Review closure:** Independent implementation and visual critics found no remaining issue in disclosure semantics, hierarchy, spacing, focus, copy reachability, overflow, compact-width behavior, or surrounding Studio surfaces. The implementation critic additionally verified `Enter` and `Space`, hidden-versus-revealed content, and zero horizontal overflow at `520` and `320` pixels in isolated Chromium.

**Final gates:** `npm run verify` passed with 95 test files, 610 passing tests and one intentional skip, production builds for Archive, Studio, and the combined local site, task-owned local startup, boundary, documentation, formatting, and high-severity dependency-audit checks. Every touched source file remains below the repository's 500-line target.

## Every created visual badge became discoverable in Archive

**Timestamp:** 2026-08-24

**Action:** Added a third Archive section, Discover, backed by a closed Git-tracked projection of every unique visualized badge concept currently created in product inputs. The catalogue contains the four available starter badges plus sixty-two additional national-park studies, deduplicates Yosemite into its available starter entry, and excludes the fifty-seven text-only common-achievement ideas because no badge artwork has been created for them.

**Result:** The user can search all sixty-six badges by title, criterion, and place and filter between `Available in Archive` and `Selected study`. Available entries reopen their existing Collection record; source studies say `Not yet published` and expose no activation, quotation, planning, 3D, installation, provider, or Studio authoring action. Discover is ephemeral and adds no Archive record, persistence version, migration, backup content, pack identity, or saying state.

**Validation:** Exact drift tests compare the safe projection with all starter and national-park authoring authorities, prove sixty-six unique IDs and Yosemite deduplication, exclude every idea-only record, close the public field shape, reject personal and authoring fields, and prove production projection modules import no catalogue-authoring code. Component tests cover counts, accent-insensitive search, availability filters, empty results, missing-thumbnail containment, and available-only navigation. The Archive media test resolves every selected-study preview from the full sixty-three-thumbnail set, while the production bundle gate requires exactly sixty-three JPEGs totaling `373,657` bytes with no file above `8,004` bytes, preventing full `896 × 896` Studio studies from crossing the boundary.

**Notes:** The implementation deliberately distinguishes discoverability from publication. A selected source study remains an authoring result rather than a `PublishedBadgeVisual`; it cannot enter Collection, planning, activation, Timeline, 3D inspection, or quotation generation until the ordinary pack, recipe, fallback, quotation-bank, admission, and installation work is complete. Private browser-local Studio work likewise remains private unless a future explicit scrubbed handoff or validated publication crosses that boundary.

**Visual verification:** Headless Chromium sweeps covered all, available-only, selected-study, Yosemite-only, and empty-search states; native `128 × 128` study rendering; Collection focus handoff; Timeline; reduced motion; and Studio candidate selection and non-destructive reprocessing at `1440 × 1000`, `1100 × 900`, and `900 × 900`. The first `1100`-pixel sweep exposed header controls wrapping after the third navigation item was added, so the existing compact two-row header breakpoint moved from `960` to `1180` pixels and the corrected Archive and Discovery surfaces were reinspected. Browser console checks reported no errors; the sole warning remains the existing Three.js `Clock` deprecation.

**Review closure:** Independent product, visual, accessibility, and correctness critics found that the first copy falsely implied an active publishing process, the widened header breakpoint left section heights subtracting the old header, the available pill clipped before a too-small type workaround, identical action names obscured their card targets, one product-spec sentence remained stale, and component tests did not prove the App-level Collection focus handoff. The final implementation says `Not yet published`, aligns `1180`- and `640`-pixel header heights without widening Timeline's existing card-layout breakpoint, shows a readable `Available` pill with full assistive context, names each action by badge, and mounts the real App to prove selection, section change, unmount, and focus; both independent critics then reported no remaining findings.

**Final gates:** `npm run verify` passed with 95 test files, 609 passing tests and one intentional skip, production builds for Archive, Studio, and the combined local site, task-owned local startup, boundary, documentation, formatting, and high-severity dependency-audit checks. The Archive bundle gate admitted exactly 63 discovery thumbnails totaling `373,657` bytes and rejected full Studio studies.

## Badge sayings became preselected historical quotations

**Timestamp:** 2026-08-24

**Action:** Removed original-prose generation, manual saying editing, the proposal card, and the separate acceptance step from the Archive; seeded each of the four starter records with one reviewed quotation; retained one `Regenerate quote` action before activation; and tightened prompt v3 plus quotation contract v2 so the provider may return only one opaque ID from a disclosed one-to-six-entry shortlist.

**Result:** A starter badge opens with its already persisted quotation, historical person, source title, and reviewed source link. Regeneration keeps that quote visible until a different exact selection has been source-bound, admitted, and durably committed, then updates the same surface without changing the art. Activation seals the exact persisted quotation and removes regeneration. Twelve exact quotation records across the four starters are locked by a manifest regression test; release review, rather than the model or URL syntax, establishes that their people are historical and their wording, attribution, work or occasion, and source links are authentic.

**Validation:** The controller retains a private immutable parsed request while giving the provider a disposable clone, then rejects fabricated IDs, altered hydration, and provider-side shortlist mutation. The Archive repository accepts no caller-supplied quotation bank: construction binds trusted lists to each exact record ID, title, and criterion, and the repository independently validates initialization defaults, regeneration results, and activation. A mounted real-App test crosses disclosure, provider controller, application service, IndexedDB, source-link rendering, activation, and durable reread; focused contracts additionally cover exact manifest values, current-ID exclusion, latest-request behavior, cancellation and failure retention, invented-bank refusal, and earned-state rejection. Headless Chromium sweeps covered Archive gallery and detail, disclosure, regeneration, activation, front/edge/back inspection, zoom and light positions, static fallback, reduced motion, and the Studio upload, reprocess, and candidate-selection surfaces at multiple desktop viewports with no task-owned browser or server left running.

**Notes:** IndexedDB v3 adds an opaque UUID quotation revision to every record. Regeneration and activation compare and rotate that token, restore and recovery refresh unearned tokens, old v2 connections are fenced, and ABA plus cross-tab restore tests prove that changing a quote away and back does not revive stale work. Default initialization is bounded and retryable only after an exact initialization conflict, applies only to compatible unearned-null records, and reruns after recovery. An earned-null record is preserved as readable evidence but never backfilled, activated, admitted by normal restore, or accepted through replacement recovery because later words cannot truthfully be assigned to an already sealed memory; startup instead requires a clearly non-restorable state-rescue handoff whose v2 evidence names the typed reason and affected record IDs. Normal restore always requires its exact safety checkpoint, and state replacement reclassifies a fresh compatible snapshot to normal restore or repair while propagating unrelated full-backup failures. Independent review found and closed mutable-shortlist trust, caller-invented bank, quotation ABA, earned-null invention, misleading rescue labeling, optional restore checkpoint, and state-rescue race paths before delivery.

**Review closure:** Harsh review additionally found that source-only rescue could become stale when one historical source was repaired while another failed, that combined missing-art and earned-null state needed to prioritize the irreversible quotation gap, that a blocked IndexedDB v3 migration could outlive a rejected or closed repository open, and that default copying initially matched a null-saying target by record ID without rechecking its title and criterion. The final implementation binds rescue evidence to a typed reason and exact affected-record set at export, confirmation, and repository compare-and-swap; prefers earned-null evidence even after art repair; includes outgoing historical sources in the final transaction condition; keeps blocked opens owned through settlement with truthful close semantics; and admits both the published default and its target against the same construction-time quotation request before initialization, restore, or recovery backfill. Focused real-IndexedDB gates cover A-to-B source-gap drift, corrupt outgoing-only art, post-inspection source repair, combined defects, blocked upgrade, close-during-blocked upgrade, and mismatched target wording at all three default boundaries.

**Final gates:** `npm run verify` passed with 91 test files, 595 passing tests and one intentional skip, production builds for Archive, Studio, and the combined local site, startup, boundary, documentation, formatting, and high-severity dependency-audit checks. Every TypeScript, TSX, JavaScript, and MJS source or test file remains at or below the repository's 500-line limit.
