# Art Style Catalogue

## Purpose and authority

This document explains the curated authoring inputs in `packages/catalogue-authoring`; the executable style records, achievement briefs, candidate plans, selected-source manifests, and prompt compiler remain the source of truth.

The authoring library exists only for Badge Studio and release curation. Archive Discover receives a separate closed projection of deliberately promoted selected-study identity, display copy, integrity-bound list thumbnails, and the integrity-bound canonical source tier that supplies activation textures; it does not receive prompts, styles, candidate plans, authoring provenance, an art-provider adapter, or permission to generate artwork while browsing, planning, activating, restoring, or replaying a memory.

The tracked authoring foundation contains `24` immutable creator-neutral style revisions and `297` projects with three planned candidates and one selected source study per concept: `63` U.S. national parks, `50` U.S. states, `50` books, `2` education milestones, and `132` named Michelin-restaurant milestones. It also retains `57` common-achievement ideas for later curation. The state selections deliberately distribute every one of the `24` styles across the fifty-study set, and the book and restaurant campaigns exercise diverse manufacturing languages across their candidate directions.

## U.S. National Parks authoring edition

The parks list is sourced from the National Park Service page [National Park System](https://www.nps.gov/aboutus/national-park-system.htm), specifically its `National Parks (63)` section, not the larger set of every National Park System unit.

The source page was last updated on 2026-07-01 and retrieved for this edition on 2026-08-23; the executable catalogue edition is `2026-07-01.nps` and declares exactly `63` individual parks.

Raw authoring definition IDs, rather than NPS website codes, identify entries inside this edition because several public NPS sites group more than one park or park-and-preserve unit under one code. They are planned pack-local IDs, not qualified Archive identity, until Phase 3 chooses a `packId` lineage and explicitly reconciles any raw ID already used by the starter pack.

Each park project carries three deliberately different candidate plans: `landmark-witness@1` makes the place recognizable through a subject-led view, `emblematic-metaphor@1` carries its emotional meaning through one original visual metaphor, and `terrain-memory@1` leads through terrain, ecology, pattern, or a remembered route.

The campaign therefore records `189` planned candidate directions and `63` selected source studies. A selection freezes the chosen candidate key, exact prompt-recipe revision, recorded prompt digest, asset digest, dimensions, accessible description, declared campaign date, sanitized generation and rights provenance, normalization recipe, and thumbnail derivative so later curation can distinguish a deliberate selection from an untraceable image. Digests prove the frozen association, not historical model-call causation.

These selected files are compact, deliberately promoted Studio source studies. Their safe metadata and list thumbnails appear as Archive Discover cards, and since D-055 each study is seeded as an activatable record through the generated discovery fixture pack; they are still not immutable `.badgepack` bytes or proof that the Phase 3 national-parks release is complete.

All `63` selected studies are normalized `896 × 896` JPEG files, each stays at or below the ordinary `256 KiB` binary threshold, and together they occupy exactly `15,211,909` bytes, about `14.51 MiB`, as deliberately promoted authoring inputs. Their deterministic `128 × 128` list thumbnails occupy `373,657` bytes in total, no thumbnail exceeds `8,004` bytes, and Studio uses the full source only for the selected detail. Across every catalogue, Archive Discover ships exactly `297` eager thumbnails plus the `296`-entry on-demand canonical source tier; the interim `384 × 384` detail tier was retired on 2026-08-27. Integrity refresh regenerates the thumbnail tier and re-binds the source manifest from the current sources before binding hashes, so stale or unrelated preview media cannot be blessed alongside changed source bytes.

Those authoring-file measurements do not prove a publish-safe runtime pack. Publication normalizes runtime art to strict PNG, and direct use of all `63` studies at `896 × 896` would charge about `193 MiB` under decoded-image admission before the composite or additional maps, above the current `64 MiB` aggregate cap.

Applying the current decoded-image admission formula to `448 × 448` derivatives leaves a plausible Phase 3 route under the budget alongside the current historical sources rather than requiring lower admission safety. That calculation does not choose the shipped quality tier or make a pack: Phase 3 must build the canonical derivatives and admit the complete closure, and still requires lazy-loading behavior, complete 3D recipes and fallback inputs, an all-parks source and presentation, canonical publication, runtime install and independent admission, starter-lineage migration and reconciliation, Archive backup v3, composite eligibility semantics, and preserved catalogue-update history.

## U.S. States authoring edition

The states list is sourced from the U.S. Census Bureau page [ANSI and FIPS Codes](https://www.census.gov/library/reference/code-lists/ansi/ansi-codes-for-states.html), specifically its state table, under executable edition `2021-10-08.census-ansi`. The edition declares exactly the fifty U.S. states and explicitly excludes the District of Columbia, Puerto Rico, and insular areas; the Census source establishes identity, abbreviation, and state FIPS code, while each art brief's landmarks, ecology, and motifs are separately curated editorial choices rather than Census claims.

Each state project has the same three versioned candidate roles as the parks campaign: recognizable `landmark-witness@1`, original `emblematic-metaphor@1`, and terrain- or route-led `terrain-memory@1`. The campaign therefore records `150` deterministic candidate directions. One primary direction per state is bound to a natively reviewed `896 × 896` JPEG source study and an integrity-bound `128 × 128` list thumbnail, with stable state identity, exact prompt association, accessible description, sanitized provenance, and content hashes.

The fifty selected studies use all `24` style revisions as their primary media, with no single collection-wide rendering look. Diversity is intentional across pixel, fiber, paint, print, ink, cartographic, paper, mosaic, geometric, ceramic, and wood families while the text-free source-art contract and physical badge renderer provide coherence.

Each state authoring record also designates a source-linked default quotation whose complete words and attribution come from a reviewed real historical figure, with a biography URL when applicable. That default is release-authoring input for eventual publication rather than a runtime promise: state studies have no admitted quotation bank, render recipe, pack identity, or activation authority yet.

The state studies cross into Archive only through the same closed safe Discover projection used for park studies. They remain selected source studies, not `PublishedBadgeVisual` values, installed definitions, `.badgepack` bytes, planned personal records, or activatable badges; publication still requires strict normalization, complete renderer and fallback closure, an authenticated quotation bank, canonical pack compilation, independent admission, installation, and backup coverage.

## Books Read authoring edition

The Books Read edition is `2026-08-25.badge-editorial-50@1`: fifty single-work reading achievements curated as plausible past or future interests, not assertions about the owner's personal history and not a universal literary canon. It excludes the separately published _Sapiens_ starter, so the complete Discover set contains `51` concepts.

Each book has three deterministic candidate directions, one selected `896 × 896` source study, one integrity-bound `128 × 128` list derivative, and a designated source-checked quotation from a real historical figure with an English-Wikipedia biography URL when applicable. The visual brief is original and recognition-oriented: it imports no cover, jacket, edition illustration, logo, blurb, or book text, and the selected study remains potential rather than evidence that the book was read.

All `150` book candidate directions compile through `badge-source-art@2` and its miniature manufacturing contract while using all `24` registered style revisions across the campaign. The exact source, derivative, prompt, style, candidate, accessible-description, normalization, provenance, and historical-quotation associations remain release-authoring inputs until a complete pack is published, admitted, and installed.

## Education milestone authoring studies

Two education projects add `Master's degree` and `University of Nebraska–Lincoln degree` to the Life Milestones Discover set beside the published bachelor's-degree starter. The master's-degree imagery is an original threshold-and-horizon metaphor. The Nebraska brief simplifies [Mueller Tower](https://historicbuildings.unl.edu/building.php?b=91), [Love Library](https://archives-spec.unl.edu/pages-to-paths/unl-library-architecture), and their campus mall relationship from official UNL architecture sources while excluding the university seal, wordmark, mascot, lettermark, and architectural microdetail.

Each education project has three deterministic candidate directions compiled through `badge-source-art@2`, one selected source study and derivative, and one source-checked historical quotation with separate quotation-source and English-Wikipedia figure-biography provenance. Those records do not assert a degree has been earned and do not create runtime quotation, preparation, activation, or personal-state authority.

## Named Michelin-restaurant studies

The Michelin Dining set contains `132` named achievements from the official selections checked on `2026-08-26`: Bay Area `41`, [New York City](https://guide.michelin.com/en/us/new-york-state/new-york/restaurants/all-starred) `69`, and [Washington, DC and surroundings](https://guide.michelin.com/us/en/district-of-columbia/restaurants/all-starred) `22`. Bay Area combines the [San Francisco and surroundings](https://guide.michelin.com/us/en/california/san-francisco/restaurants/all-starred) result with qualifying restaurants from both [California result pages](https://guide.michelin.com/us/en/california/restaurants/all-starred) under the Metropolitan Transportation Commission's nine-county definition. Each authoring record binds its individual Guide page and current distinction for visit-date eligibility, cuisine and locality for search, and a paraphrased factual interior, dish, or cultural cue for the art brief; the safe Discover projection exposes the individual official listing as the reference and visual basis but not the rating ledger.

Restaurant names and current distinction facts identify the achievement, but the edition reproduces no Guide prose, source pixels, restaurant logo, signage, menu typography, chef likeness, Michelin logo, star pictogram, rating mark, or red commercial-guide trade dress. Every source is new deterministic geometry whose `3–5` broad forms abstract the recorded cue, whose exact design JSON and renderer bundle are integrity-bound, and whose criterion checks the visit date so a later Guide change cannot rewrite a dated personal memory.

All `396` dining directions compile through `badge-source-art@2`; the `132` selected named studies share one reviewed Oscar Wilde quotation from _A Woman of No Importance_ as release-authoring input, with its Project Gutenberg source and English-Wikipedia biography association. These are source-grounded achievement studies, not an installed current-rating service, endorsement, or claim that any visit occurred.

## Style library

Historical movements and material processes are translated into general visual qualities. Prompts never request imitation of a named creator, recognizable artwork, protected character, brand trade dress, or existing composition.

| Style revision                    | Family       | Intended qualities                                                                                                                  |
| --------------------------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------- |
| `pixel-cluster-landscape@1`       | Pixel        | Deliberate hard-edged clusters, a controlled palette, value steps, and restrained dithering                                         |
| `thread-painted-embroidery@1`     | Fiber        | Directional long-and-short stitch, satin stitch, couching, and tactile pictorial depth                                              |
| `plein-air-broken-color@1`        | Paint        | Historical impressionist qualities of broken color, visible bristle marks, atmosphere, and fleeting light without creator imitation |
| `relief-blockprint@1`             | Print        | Bold carved marks, compact inks, and monumental silhouettes                                                                         |
| `stained-glass-field@1`           | Mosaic       | Irregular luminous color cells and disciplined internal joins                                                                       |
| `monoprint-ink-wash@1`            | Ink          | Ink pools, dry brush, transferred texture, negative space, and one restrained accent                                                |
| `contour-atlas-ink@1`             | Cartographic | Unlabeled topographic rhythm, watersheds, route movement, and precise mineral linework                                              |
| `mineral-line-engraving@1`        | Print        | Fine hatching, stipple, warm paper, and restrained oxidized-earth color                                                             |
| `aquatint-atmosphere@1`           | Print        | Tonal grain, etched accents, deep distance, and quiet environmental light                                                           |
| `risograph-overprint@1`           | Print        | Translucent spot inks, selective overprint, and subtle registration energy                                                          |
| `matte-gouache-editorial@1`       | Paint        | Opaque matte shapes, mature color blocking, and selective dry-brush detail                                                          |
| `woven-tapestry-field@1`          | Fiber        | Interlocking woven fields, visible warp and weft, and warm patient craftsmanship                                                    |
| `cut-paper-strata@1`              | Paper        | Crisp layered silhouettes, fibrous edges, and sculptural depth through value steps                                                  |
| `cyanotype-field-study@1`         | Print        | Prussian blue, pale photographic traces, and intimate scientific observation without labels                                         |
| `watercolor-naturalist-study@1`   | Paint        | Transparent blooms, exact observation, ecological supporting forms, and delicate dry brush                                          |
| `charcoal-pastel-weather@1`       | Paint        | Broad charcoal masses, lifted light, selective pastel, and moving weather                                                           |
| `screenprint-night@1`             | Print        | Layered nocturnal silhouettes, deep inks, and one scene-local luminous accent                                                       |
| `mosaic-tesserae-field@1`         | Mosaic       | Directional hand-set tesserae, stone color, and sparse environmental glass highlights                                               |
| `fresco-mineral-pigment@1`        | Paint        | Matte mineral pigment, weathered edges, and broad timeless geological masses                                                        |
| `prismatic-geometric-symbolism@1` | Geometric    | Interlocking planes, concentrated prismatic color, and a poetic spatial metaphor rather than a logo                                 |
| `ceramic-underglaze@1`            | Ceramic      | Brushed cobalt, iron, copper, clay hues, and subtle kiln variation within the image                                                 |
| `wood-marquetry-landscape@1`      | Wood         | Fitted natural grain shapes whose direction reinforces land, water, and atmosphere                                                  |
| `scratchboard-nocturne@1`         | Ink          | Fine light carved from darkness with strong thumbnail silhouettes                                                                   |
| `luminous-ligne-claire@1`         | Ink          | Clean contour, immense airy scale, luminous color, and an original journey-like visual language                                     |

The library intentionally ranges across pixel art, embroidery, painterly and impressionist qualities, printmaking, ink, cartography, paper, mosaic, ceramic, and wood instead of forcing one collection-wide illustration style.

Collection coherence comes from exact briefs, meaningful subject recognition, text-free source composition, and the shared physical renderer, not from making every picture look alike.

## Owner cadence preference

The owner's love of Moebius maps to the registered creator-neutral `luminous-ligne-claire@1` direction: clean contour, vast airy space, quiet science-fantasy scale, luminous restrained color, and an original journey-like composition rather than copied characters, vehicles, worlds, or panels.

For each new curated campaign with at least eight concepts, authoring plans target this direction in roughly `25%` of candidate trios and roughly `10%` of primary selections when the subject supports it. This makes the language recurrent without turning it into a collection-wide default; each use still needs subject-specific motifs and a miniature-ready treatment, the campaign must retain a broad primary-style mix, and immutable existing studies are not relabeled or regenerated merely to satisfy the cadence.

The machine-readable policy lives in `frequentArtStylePreferences`, and `recommendedFrequentArtStyleUsage` converts a future campaign's exact concept count into reviewable candidate and primary targets. The first consuming queue is the unselected common-achievement idea bank, where the direction now appears in `14 / 57` candidate trios and `6 / 57` primary suggestions; that queue also keeps at least `18` primary styles in play and caps any one primary style at `15%`. Production prompts continue to use the registered creator-neutral style name and directives rather than naming or asking a model to imitate an artist.

A future deterministic code-native badge counts as a luminous-ligne-claire primary only when its rendered recipe visibly implements clear contour, generous negative space, restrained luminous fields, and subject-specific speculative scale; assigning the style ID to generic geometry is not evidence that the preference reached the pixels.

## Deterministic prompt recipe

`badge-source-art@1` compiles a prompt deterministically from one normalized `BadgeArtBrief`, one `CandidatePlan`, one immutable style revision, and the fixed candidate-role direction.

The compiler writes the same sections in the same order: source-art instruction, achievement reference data, candidate role, style directives, structured composition, required and excluded motifs, and the output contract.

Achievement title, criterion, meaning, theme cues, mood, palette, focal subjects, supporting elements, safe zone, viewpoint, depth, and narrative beat are treated as bounded reference data rather than instructions that may change the contract.

Every compiled prompt ends with the same source-art requirements: square and full bleed, essential content inside the declared safe zone, no typography, and no finished badge, rim, border, bevel, thickness, reverse face, cast shadow, pedestal, mockup, presentation background, object-level material, or movable studio lighting.

Scene-local light remains valid subject matter. Shape, material, edge, relief, depth, crop, border, and inspection lighting remain structured renderer-owned work applied later in Badge Studio.

The compiler requires visible NFC text, rejects blank values plus control and format characters with exact candidate and field context, and refuses a prompt larger than `16 KiB` of UTF-8 rather than silently normalizing, truncating, or changing the recipe.

`badge-source-art@2` is the current recipe for newly generated or deliberately refreshed source art. It appends a mandatory `small-badge-face@1` miniature-production contract after the complete immutable v1 prompt; explicit compiler dispatch retains both revisions, the existing park and state studies remain associated with their recorded v1 prompts until their pixels are deliberately regenerated and reviewed, and all fifty book, two education, and `132` named dining studies record exact v2 compilation.

The miniature proof uses a `32 mm` reference face and a `48 × 48` pixel reduction. A composition uses `3–5` primary forms, no more than `3` supporting accents, and no more than `6` visually distinct color families; a recognition-critical form or mark is at least `1 mm` or `3.125%` of the face width, and a recognition-critical negative gap is at least `0.8 mm` or `2.5%` of the width. A noncritical construction line follows the registered treatment floor—`0.35 mm` for stained-glass or cloisonne joins and `1.25 mm` for embroidery, for example—before vendor-specific production preflight.

Anything that disappears at the proof size is merged, enlarged, or omitted. Microdetail, hairlines, dense hatching, stipple fields, tiny knots or threads, minute tesserae, scattered specks, dense foliage, tiny figures, and background objects that require magnification are excluded even when a v1 style directive permits finer surface texture.

The same diverse style library remains available, but v2 translates each style family into a production-compatible face language: embroidery or woven-thread fields, stained-glass or cloisonné cells, kiln-enamel color blocking, spot-color relief printing, broad etched line and fill, stepped woven blocks, die-cut inlay, etched contour inlay, hard-enamel cells, underglaze color fields, or wood marquetry and fitted inlay. Those are flat source-art grammars, not depictions of a manufactured object; the recipe still prohibits typography, a finished badge or patch, rim, border, backing, clasp, thickness, cast shadow, mockup, and other renderer-owned construction.

Run `npm run catalogue:prompts -- acadia` to print the exact recorded v1 selected Acadia prompt as JSON, pass several park slugs to inspect several selected prompts, or omit slugs to print all `63`. Run `npm run catalogue:state-prompts -- washington california` for recorded state prompts, pass other state selectors for a focused set, or omit selectors to print all `50`. Run `npm run catalogue:new-prompts -- masters-degree 1984 san-francisco-surroundings` to print exact v2 prompts from the fifty-book, education, and regional-dining campaigns. New candidate generation requests the current v2 recipe, while provenance replay dispatches the exact recorded revision.

## Common-achievement idea bank

The idea bank is a release-authoring queue rather than an Archive suggestion or discovery feed. Its `57` entries span adventure `10`, community `7`, craft `5`, creative `8`, learning `7`, life `9`, nature `5`, and wellbeing `6`.

Examples include seeing the northern lights, completing a marathon, learning to swim, reading fifty books, earning a degree, shipping an app, making a garment, hosting a family reunion, mentoring someone, changing careers, and sustaining a lifelong friendship.

Each idea carries a stable ID, title, honest criterion, three theme cues, and three suggested style IDs. All `171` idea-and-role combinations resolve to distinct candidate keys and compile through the same deterministic prompt contract. An idea has no selected art, presentation, `PackRef`, lifecycle state, or personal record until a later Studio curation and publication unit supplies them.

## Planning-only expansion briefs

The repository records source-boundary briefs for later catalogue work without representing those plans as authored or created badges. The municipality briefs identify exactly `281` incorporated Washington cities and towns in the April 1, 2026 Office of Financial Management edition and `483` incorporated California cities and towns in the 2026 state roster; both exclude unincorporated places and require stable Census place-FIPS reconciliation before authoring.

The planning books brief separately freezes PBS's 2018 _Great American Read_ as one edition of `100` novels or series, preserves PBS's single-work-versus-series scope, and imports no cover art, descriptions, blurbs, or book text. It is distinct from the selected fifty-work Badge editorial edition, remains a recognition-oriented research snapshot rather than selected art, and is not a universal canon or evidence of what the owner has read.

The Tokyo brief is a non-exhaustive Badge-editorial seed of `34` durable places informed by the official GO TOKYO guide. Its landing-page research is not item-level provenance: every place must bind to its own current GO TOKYO or venue-authority URL before an authoring record or art brief may be created.

Beyond the selected `132`-restaurant Guide edition, the planning catalogue retains one user-entered restaurant milestone template for a location and visit date outside that bounded selection. The repository includes no runtime scraper, redistributed Guide prose or photography, restaurant branding, Michelin logo, star pictogram, rating mark, or red commercial-guide trade dress; a later refresh must repeat the source inventory, cue research, rights exclusions, miniature review, and exact provenance gates instead of silently presenting stale distinctions as current.

Beyond the two selected education studies, the life-milestone brief offers `64` optional reflection prompts across eight broad areas. They are non-normative and personal-honesty-only, with no age deadlines, hierarchy, score, diagnosis, prescribed family structure, required marriage or parenthood, required homeownership, or external verification.

## Expansion and revision protocol

Every curated catalogue expansion reviews style-family, medium, mood, subject, palette, and composition coverage before assigning candidate styles.

When a meaningful visual gap exists under the current v1 ID-only lookup, add a new creator-neutral style ID and document the added capability. When the existing library is sufficient, record that conclusion in the release decision or devlog instead of silently treating the review as unnecessary.

An existing `{ styleId, revision }` is immutable once used. Never edit its directives, label semantics, or prompt behavior in place; supporting revision `2` of an existing ID first requires versioned candidate and common-idea references plus a lookup and compiler that retain revision `1`, and only then may the new explicit revision coexist.

The candidate-role revision remains a single-dispatch v1 contract. Prompt compilation now dispatches explicitly between immutable `badge-source-art@1` and miniature-production `badge-source-art@2`; introducing any later revision must keep both prior compilers callable and must never rewrite recorded prompt behavior.

New styles must retain the flat, text-free, full-bleed source-art contract and must not bake renderer-owned physical construction into generated pixels.

Candidate assignments should create useful contrast for the achievement rather than rotate styles mechanically. A new style earns its place by enabling a genuinely different and appropriate proposal, not by increasing a count.
